# 2026-03-13 - 记忆系统修复 + WebChat 同步问题 + 星鉴推送修复 + 星鉴流水线首次完整运行

## 重要成就

### ✅ 修复 memory-lancedb-pro 的 memory_list 和 memory_stats 工具
**问题**：两个工具返回 0 条记忆，但自动召回和自动捕获正常工作

**调查过程**：
1. 验证数据库完整性：337 条记忆存在
2. 验证插件加载：正常注册
3. 验证自动召回：日志显示 "injecting 3 memories into context"
4. 添加调试日志：发现 `agentId: undefined, scopeFilter: ['global']`
5. 定位根本原因：`resolveRuntimeAgentId()` 返回 `undefined`

**根本原因**：
- `context.agentId` 未设置
- `runtimeCtx.agentId` 不存在
- `runtimeCtx.sessionKey` 解析失败
- 导致 `scopeFilter` 只包含 `['global']`，而数据库记忆都是 `scope=agent:main`

**修复方案**：
在 `~/.openclaw/workspace/plugins/memory-lancedb-pro/src/tools.ts` 中：
```typescript
// memory_list 工具（约 1168 行）
const agentId = resolveRuntimeAgentId(context.agentId, runtimeCtx) || 'main';

// memory_stats 工具（约 1050 行）
const agentId = resolveRuntimeAgentId(context.agentId, runtimeCtx) || 'main';
```

**验证结果**：
- ✅ `memory_list` 返回 5 条记忆
- ✅ `memory_stats` 显示 316 条记忆

**上游提交**：
- Issue #186: https://github.com/CortexReach/memory-lancedb-pro/issues/186（已关闭）
- PR #187: https://github.com/CortexReach/memory-lancedb-pro/pull/187（已关闭）
- 官方在同一天也独立修复了此 bug (commit d73bff4)
- 已切换到 main 分支使用官方修复

---

### ✅ 诊断并报告 WebChat 同步问题

**问题 1**：WebChat UI 不会实时显示来自其他渠道（如 Telegram）的消息，需要手动刷新

**测试结果**：
- ✅ WebChat → Telegram：实时同步
- ❌ Telegram → WebChat：需要手动刷新

**根本原因**：WebChat UI 的 WebSocket 事件订阅或处理有 bug

**上游报告**：Issue #44619: https://github.com/openclaw/openclaw/issues/44619

---

**问题 2**：AI 回复不会广播到所有共享 session 的渠道

**测试结果**：
- ✅ Session 是共享的（消息在同一个 session 历史中）
- ❌ 从 WebChat 发送的消息触发的回复，只发送到 WebChat，不发送到 Telegram

**根本原因**：消息路由问题，回复只发送到 originating channel

**上游报告**：Issue #44758: https://github.com/openclaw/openclaw/issues/44758

**临时解决方案**：在 WebChat 中手动刷新查看新消息

---

### ✅ 修复星鉴流水线推送机制

**问题**：星鉴流水线运行 25 分钟，没有任何推送消息到监控群

**调查过程**：
1. 验证 main agent 可以发送消息到监控群 ✅
2. 验证 subagent 有 message 工具权限 ✅
3. 测试 subagent 发送消息到监控群 ✅
4. 发现流水线 isolated session 没有实现推送逻辑 ❌

**根本原因**：
- AGENTS.md 规定：agent 应自己发送开始/进度/完成消息
- 但 isolated session 的 task 中没有明确的推送指令
- 启动模板只有"通知要求"描述，没有具体的 message 工具调用示例

**修复方案**：
更新 `~/.openclaw/skills/stareval/references/launch-template.md`，在每个步骤添加明确的推送指令：

```text
**推送要求（必须执行）**：
1. 开始时立即发送：
   message(action="send", channel="telegram", target="-5131273722", message="【星鉴 Step X】<步骤名> - 开始")
2. 完成时立即发送：
   message(action="send", channel="telegram", target="-5131273722", message="【星鉴 Step X】<步骤名> - 完成\n\n<关键信息>")
```

**修复范围**：
- Step 2: Gemini 研究宪法
- Step 3: Review 主方案
- Step 4: Gemini 一致性复核
- Step 5: GPT/Review 仲裁

---

### ✅ 星鉴流水线首次完整运行（Main 直接编排模式）

**问题发现**：
- Isolated session 模式无法正常执行流水线
- Agent 不理解应该如何执行流程
- 工具调用参数缺失（write 工具没有 content 参数）

**解决方案**：
改用 **Main 直接编排模式**，在当前会话中逐步 spawn 各个 agent

**优势**：
- ✅ Main 可以控制每一步
- ✅ Main 可以在每步之间发送推送消息
- ✅ Main 可以处理错误并立即修复
- ✅ 符合 AGENTS.md 的"Main 编排"原则

**执行流程**（2026-03-13 14:57 - 15:24）：
1. ✅ **Step 1**：任务分级（D级深度报告）
2. ✅ **Step 1.5**：Gemini 快速扫描（1m55s）
   - 产出：问题清单、盲点清单、待验证假设
   - 报告：`~/.openclaw/workspace/agents/gemini/reports/system-prompt-9-layer-scan-20260313.md`
3. ✅ **Step 2A**：OpenAI 宪法简报（2m11s，首次失败后重试成功）
   - 产出：范围、定义、判定标准、禁止项、证据门槛、输出格式
   - 报告：`~/.openclaw/workspace/agents/openai/reports/system-prompt-9-layer-constitution-20260313.md`
4. ✅ **Step 2B**：NotebookLM 深度研究（5m57s）
   - 产出：执行摘要、架构层级分析、假设验证、优化建议
   - 报告：`~/.openclaw/workspace/agents/notebooklm/reports/system-prompt-9-layer-deep-research-20260313.md`
   - 核心发现：
     - ✅ 假设1成立（L8优先级高于L7）
     - ❌ 假设2证伪（L8不能修改L1-L6）
     - ⚠️ 假设3/4待验证
   - 三大风险：上下文膨胀、指令冲突、安全边界模糊
5. ⏳ **Step 3**：Claude 复核优化（进行中，首次超时后重试）

**推送机制验证**：
- ✅ Main 成功发送所有阶段推送到监控群
- ✅ Gemini 成功发送完成消息
- ✅ OpenAI 成功发送完成消息
- ✅ NotebookLM 成功发送完成消息
- ✅ 推送机制完全正常工作

**遇到的问题**：
1. **OpenAI 首次执行返回空结果**（0 tokens）
   - 解决：简化任务描述后重试成功
2. **Claude 首次执行超时**（10分钟，0 tokens）
   - 解决：简化任务描述后重试（进行中）

**问题模式**：
- 某些 agent 首次 spawn 可能遇到初始化问题
- 表现为 0 tokens + 空结果或超时
- 重试通常能解决

---

## 教训

1. **问题定位**：不要假设问题的根源，要通过测试验证
2. **文档优先**：遇到 OpenClaw 问题应该先查询 openclaw-docs notebook，而不是直接读取本地文件
3. **双向测试**：测试双向通信才能确定问题方向
4. **记录完整**：修复完成后立即主动记录，不等提醒
5. **推送机制**：模板中的"通知要求"不够明确，需要具体的工具调用示例
6. **执行问题**：知道规则但不执行，是深层执行问题，必须改进
7. **编排模式**：Main 直接编排比 isolated session 更可控、更可靠
8. **Agent 重试**：首次 spawn 失败时应该立即重试，不要放弃

## 其他

- 晨星最初提到"Telegram 和 WebChat UI 信息同步的问题"
- 通过调查发现了两个独立的 bug：WebChat UI 实时更新 + 消息路由广播
- 同时发现并修复了星鉴流水线的推送机制问题
- 首次成功运行星鉴流水线（Main 直接编排模式）
- 这次调查暴露了多个系统性问题，都已修复或报告

---

## ✅ 固化结论：subagent announce 与 message 是两条独立链路（2026-03-13 16:30）

### 调查背景
在星鉴流水线首次完整运行后，发现：
- main 可以稳定补发监控群消息
- subagent completion/announce 能回到 main
- 但各 agent 自己声称已发送的 Telegram 消息，在真实 `sessions_spawn(mode=run)` 流程里大量缺失

### 最小复现验证
执行最小测试：
1. spawn 一个 gemini subagent（mode=run）
2. 要求它向监控群发送 start 消息
3. 写一个测试文件
4. 再向监控群发送 done 消息
5. 正常结束

**验证结果**：
- ✅ 子 agent 正常完成
- ✅ 测试文件成功写入：`agents/gemini/reports/subagent-message-test-20260313.txt`
- ✅ main 收到 completion/inter-session announce
- ❌ Telegram 发送日志中没有抓到这次 subagent 向监控群发送的对应 sendMessage 记录

### openclaw-docs notebook 结论
通过 notebooklm skill 查询 `openclaw-docs` notebook，得到结论：
1. `sessions_spawn(mode=run)` 的 subagent completion/announce 与 `message(...)` 是**两条独立链路**
2. subagent 可以**成功完成 + announce 回 parent/main**，同时它自己的 `message(...)` 仍可能不投递
3. announce 有独立的 parent-handoff / retry / queue 机制，普通 `message(...)` 不走这套回传兜底
4. **官方可靠模式**：关键通知不要依赖 subagent 自推；应由 parent/main 统一 relay/broadcast

### 最终结论
- ✅ `announce -> main` 是更可靠的链路
- ❌ `subagent message(...) -> Telegram` 不能当作可靠主链路
- fallback / timeout / auth error 会放大问题，但不是唯一原因
- 真实流水线里：**main 统一补发应作为主链路，agent 自推仅作为 best-effort 辅链路**

### 已落地修正
1. 更新 `AGENTS.md`：
   - main 发往监控群 / 晨星 DM = 可靠主链路
   - agent 自己的职能群通知 = best-effort 辅链路
2. 更新 `~/.openclaw/skills/stareval/SKILL.md`：
   - 关键进度不再依赖子 agent 自推
   - 只要文件落地 + completion/announce 回主链，main 就应立即补发监控群

### 教训
- 不能把“模型文本里说已发送通知”当成实际投递成功
- 不能把单独 message 测试成功，等同于真实 run 场景链路稳定
- 对于多智能体流水线：**结果生产归 subagent，外部可见性归 main**

### ✅ 全局约定生效：所有流水线通知统一采用 main-first 主链路（2026-03-13）

经晨星确认，以下约定从今天起适用于所有流水线与多智能体编排：
- `main` 发往监控群 / 晨星 DM = 可靠主链路
- agent 自己的职能群或监控群自推 = best-effort 辅链路
- 不再把 agent 自推是否成功，视为完成通知是否可靠的判断标准
- 关键进度、结果、失败、告警，统一以 `main` 的补发 / 统一交付为准

已同步应用到：
- workspace `AGENTS.md`
- `~/.openclaw/skills/stareval/SKILL.md`
- `~/.openclaw/skills/stareval/references/launch-template.md`
- `~/.openclaw/skills/wemedia/SKILL.md`
- `~/.openclaw/skills/starchain/SKILL.md`（原规则已基本一致，继续沿用 main-first）

### ✅ 旧版参考文档已同步对齐 main-first 通知规则（2026-03-13）

为避免执行入口已更新但旧版参考材料仍误导，继续清理并同步了历史 flowchart / contract 文档中的通知表述：

已更新：
- `~/.openclaw/skills/stareval/references/pipeline-v1-5-contract.md`
- `~/.openclaw/skills/stareval/references/PIPELINE_FLOWCHART_V1_2_EMOJI.md`
- `~/.openclaw/skills/stareval/references/PIPELINE_FLOWCHART_V1_5_EMOJI.md`
- `~/.openclaw/skills/stareval/references/PIPELINE_FLOWCHART_V2_0_EMOJI.md`
- `~/.openclaw/skills/starchain/references/pipeline-v2-7-contract.md`
- `~/.openclaw/skills/starchain/references/pipeline-v2-8-contract.md`

统一替换后的口径：
- agent 自推 = best-effort
- main = 可靠主链路
- 不再把 agent 自推是否成功视为监控群可靠通知的前提
