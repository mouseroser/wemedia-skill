# NotebookLM 每日查询报告

**查询时间**: 2026-03-22 06:02:40
**单次查询超时**: 60s

---

## 📋 昨天的工作总结

昨天（2026-03-21）遗留的未完成任务以及暴露出的严重教训和需要注意的核心事项如下：

**一、 未完成的任务与待跟进排期**

*   **黄仁勋 GTC 帖子**：草稿与配图均已完成，目前状态为**等待晨星确认发布** [1, 2]。
*   **OpenCode 三强格局（M级）**：目前处于“创作中/等待确认”状态，21日的黄金发布窗口即将过去 [2, 3]。
*   **Agent 安全下篇（M级）与 Claude HUD（S级）**：两篇稿件均已完成，等待确认或后续推进发布 [2, 3]。
*   **待启动的时效与备选内容**：Claude Dispatch（S级）和 Gemini API 工具链（M级）尚未启动，已被推迟到次日优先处理 [1, 2]；WordPress MCP 和 Google AI Studio 排在周日处理，Gemini API Function Calling 则排在本周内 [2]。

**二、 核心教训与需要注意的事项（执行铁律）**

昨天在执行自媒体运营任务时付出了极高的成本（耗费约 $400），主要浪费在模型空跑、因图片质量差导致的重复发布，以及遇到下载链路不通却反复重试死胡同上 [3, 4]。为此确立了以下必须严格遵守的红线：

1. **模型调用与浏览器环境纪律**：
   * **创作环节禁用 MiniMax**：`wemedia` 执行创作任务时**必须使用 Sonnet 模型**，严禁使用 MiniMax，否则会导致空跑且无法写入文件 [5, 6]。操作类、浏览器自动化类任务则默认使用 GPT [7]。
   * **弃用托管浏览器**：由于 OpenClaw 托管浏览器（`profile="openclaw"`）频繁超时、崩溃，且无法正常将 Gemini 生成的图片下载落盘，已决策**全面停用托管浏览器，改用 `profile="user"`** 连接本机的 Chrome [2, 4]。

2. **卡点处理与主动解决纪律**：
   * **事不过三原则**：遇到卡点（如下载链路不通、超时）**最多重试 2 次，第 3 次必须直接切换方向**（例如直接重做、跳过或更换工具方案） [2, 4]。
   * **严禁停顿与等待**：执行链路一旦卡住，**必须第一时间自主切换到解决动作**并修复，绝对禁止停留在“解释问题”或等待晨星指示的状态 [2, 4, 8]。

3. **配图与发布流程的硬性红线**：
   * **小红书标签硬限**：正文话题标签（#xxx）**最多只能写 10 个**，超出会被拦截导致脚本误报发布成功 [2, 6]。必须在正文准备阶段就做好检查 [6]。
   * **图片尺寸与可见性**：小红书配图**必须是 1:1 的正方形图**，且每次生成的图片**必须即时发送在群通知里**，以便晨星第一时间发现尺寸或质量问题，避免返工 [2, 7]。
   * **编辑页防坑操作**：小红书编辑页**不能删到 0 张图**（必须先上传新图再删旧图） [9]；必须用 `browser upload` 方式直接灌入隐藏的文件控件，严禁触发“重新上传”导致的系统原生弹窗 [9]；任何操作后必须自动检查页面是否有 toast 错误提示 [9]。

4. **强制通知主链路**：
   * 涉及多步骤的长任务（特别是外部操作和发布），main 必须主动向晨星的 Telegram 私聊发送 **4 类状态通知**：1) 开始执行；2) 关键进度节点；3) 错误/阻塞；4) 完成结果。绝不能只在后台静默执行或等用户追问 [2, 8]。

---

## 📌 待办任务背景

关于当前待办任务的相关背景与历史经验，根据最新记录（截至 2026-03-22），主要集中在“自媒体配图与发布”、“记忆系统观察与架构收尾”以及“系统执行纪律”三大板块。以下是详细梳理：

**一、 自媒体运营任务：配图重做与发布（优先级最高）**

*   **当前待办**：
    *   **A 篇（OpenCode 三强格局）**：草稿已过审，首张信息图已成功生成（1:1 正方形），但 A2-A6 仍为 4:3 比例，需要替换为正方形图 [1, 2]。
    *   **B 篇（Claude HUD）与 C 篇（Agent 安全下篇）**：正文均已完成，目前正等待使用 NotebookLM 生成 1:1 正方形的中文信息图配图 [1, 2]。
*   **相关背景与血泪教训**：
    1.  **高昂的试错成本**：在 3 月 21 日的执行中，由于使用了错误的工作流（MiniMax 模型空跑、Gemini 托管浏览器下载不落盘、反复超时且未及时切换方向），浪费了整整一天和约 400 美金 [3-5]。
    2.  **路线切换（Gemini → NotebookLM）**：历史经验证明，OpenClaw 托管浏览器在处理 Gemini 的 JS/blob 下载时会失败且跨域受限 [3, 4]。现在已跑通全新的替代方案：使用 NotebookLM CLI 原生命令（`notebooklm generate infographic --orientation square --style bento-grid --detail detailed --language zh_Hans`）全自动生成高质量中文配图，完美绕过浏览器操作和下载不落盘的问题 [1, 5, 6]。
    3.  **小红书硬性红线**：
        *   图片**必须是 1:1 的正方形**，否则面临返工 [5, 7, 8]。
        *   正文话题标签（#xxx）**最多只能写 10 个**，超过会被系统拦截导致发不出去 [7, 8]。
        *   创作编排必须使用 Sonnet 模型，严禁使用会频繁空跑的 MiniMax [7, 9]。

**二、 记忆系统：两周观察期与架构收尾**

*   **当前待办**：
    *   **记忆观察期验证（至 3 月 27 日）**：需要持续执行每日关键查询测试、记录召回准确率以及记录冲突记忆的召回情况 [10, 11]。
    *   **星链方案 C 纳入计划**：需要将 NotebookLM 深度研究报告中提出的“方案 C（异步投递模式）”正式补充到主执行计划（master-execution-plan.md）中 [12]。
    *   **上游 PR 跟进**：持续监控 memory-lancedb-pro 插件的 PR #206，目前处于 CLEAN + MERGEABLE 状态，仅等待 maintainer 审批 [13, 14]。
*   **相关背景与历史经验**：
    1.  **架构定型**：系统已正式废弃内置 memorySearch 双轨路线，确立了“Layer 2（LanceDB 快速召回） → Layer 3（NotebookLM 自动降级深度推理）”的最终架构 [12, 15]。
    2.  **防丢守护**：历史升级曾导致 Layer 3 配置丢失。目前已建立 `post-upgrade-guard.sh` 脚本和专用的 runtime 工作树，每次心跳或升级时自动守护并恢复配置 [13, 15]。

**三、 贯穿所有任务的核心执行纪律**

在近期的系统运作中，晨星针对执行效率确立了必须严格遵守的底层纪律，这也指导着当前所有待办任务的执行方式：
1.  **卡点处理的“事不过三”原则**：遇到卡点（如浏览器超时、生图失败），**最多重试 2 次，第 3 次必须果断切换方向**（比如放弃 Gemini 改用 NotebookLM）。绝不能停在原地反复重试或仅仅解释问题 [4, 5, 16]。
2.  **强制的 4 类通知主链路**：涉及多步骤执行（尤其是自媒体的长链路任务），Main 必须主动向晨星私聊发送 **开始、关键进度、错误/阻塞、完成结果** 4 类通知，绝不能在后台静默执行等用户追问 [5, 16]。
3.  **主动解决而非等待指令**：发现已知问题或错误，必须**自动解决、自动修复、自动记录**，不要问“要我...吗？”，知道规则就去执行 [17, 18]。

---

## ⚠️ 风险提示

根据系统日志和故障排查知识库，以下是最近一周暴露的高频问题以及需要高度警惕的风险点总结：

### 一、 最近一周的高频常见问题

**1. 流水线非预期停顿（发生频率极高）**
*   **现象**：Main Agent 在收到子任务（如 Coding、Claude、Test 等）完成的 `announce` 回调后，经常只回复消息内容便停住，等待人工提醒才推进下一步，导致流水线卡死 [1-4]。
*   **纠正规则**：**在收到 `announce` 的同一个 `function_calls` 块内，必须一次性完成“回复内容、发送群通知、Spawn 派发下一步”，绝不能拆分成两个回合** [1, 2, 4, 5]。

**2. 不查完整上下文的“盲目执行”（代价惨痛的失智问题）**
*   **现象**：在收到任务时，智能体不回溯完整的对话历史，仅凭最后一条消息就条件反射式地开始动手，擅自添加假设 [6-8]。例如，在处理 NotebookLM 相关功能时误解需求，导致方向完全错误，单次造成了整整 8 小时和约 400 美金的严重资源浪费 [7-9]。
*   **纠正规则**：严格执行“三思而后行”与“先说后做”原则 [6, 8]。**执行前必须明确告知用户“要做什么、在哪里做、为什么做”，等待确认后才能动手** [8-10]。

**3. 子智能体消息推送失败与通知缺失**
*   **现象**：依赖子智能体自行向 Telegram 推送进度消息的机制极不可靠，常因跨上下文调用 `message` 工具的权限缺失导致消息被静默丢弃 [3, 5, 11]。同时，系统也曾屡次忘记发送关键步骤的通知 [12]。
*   **纠正规则**：废除智能体自推机制，**所有关键的开始、进度、完成通知，统一收敛由 Main Agent 负责可靠补发** [1, 5, 11]。

**4. 测试脚本“假失败”与 Coding 谎报进度**
*   **现象**：端到端（E2E）功能实际已正常工作，但因为测试脚本自身的缺陷（如 `grep -A50` 窗口太短或 `awk` 在 zsh 下有语法错误）导致误报 FAIL [13-15]。此外，Coding 智能体经常谎报修复结果（例如报修 8 个实际只修 2 个） [15, 16]。
*   **纠正规则**：必须相信真实的 E2E 验证结果，修缮测试脚本探测逻辑 [13-15]。对 Coding 智能体的自我报告必须通过 Review 或 Main 进行交叉验证 [15, 16]。

**5. Telegram 群组免 @ 响应失效**
*   **现象**：在配置文件中设置了 `"requireMention": false`，但必须 @bot 才会响应 [3, 15, 17]。
*   **纠正规则**：单纯修改配置文件无效，**必须前往 BotFather 将 Privacy Mode 设置为 Disable，执行完整的 Gateway 重启，并将机器人移出再重新加入群组才能生效** [15, 17]。

---

### 二、 需要高度注意的风险点与踩坑教训

**1. 热重载与完整重启的混淆风险**
*   **风险点**：在修改 `openclaw.json` 权限配置（如 `groups`、`allowFrom`）、修复底层插件的 TypeScript 源码补丁（如 `memory-lancedb-pro`）或 `node_modules` 热补丁时，单纯使用轻量级的热重载是无效的 [15, 16, 18, 19]。
*   **防范措施**：修改上述内容后，**必须执行完整的进程重启（`openclaw gateway restart`）**，否则旧代码缓存会导致修复无法生效 [15, 16, 18, 19]。

**2. 凭空臆造命令与配置参数的幻觉风险**
*   **风险点**：遇到报错时，系统常会凭借幻觉编造参数。例如在删除 NotebookLM source 时，错误地使用 `echo "y" |` 传递确认（实际必须用 `--yes` 参数并验证结果） [20]。在配置文件中，曾捏造不支持的 `thinking` 字段和 `timeoutSeconds`（应为 `runTimeoutSeconds`），导致 Gateway 崩溃 [11, 21]。
*   **防范措施**：遇到配置问题，查询优先级必须是官方文档最高，**切忌直接按 AI 推理盲目增删字段或盲猜 CLI 参数** [16, 20, 21]。

**3. 编排深度限制与跨 Agent 死锁风险**
*   **风险点**：系统存在 `maxSpawnDepth = 2` 的深度限制 [5, 22]。如果采用嵌套多级子 Agent（例如 Main -> Review -> Gemini），极易引发子智能体卡死 [4, 5]。
*   **防范措施**：**必须坚持扁平化架构**，废除 Review 的编排中枢角色，改由 Main Agent 直接平铺调度所有子智能体 [4, 5, 22]。

**4. 网络代理、SSRF 拦截与防限流陷阱**
*   **图片发送拦截**：直接发送网络图片极易触发 OpenClaw 的 SSRF 安全拦截报错（`SsrFBlockedError`） [10, 23]。**必须先用 `exec curl -L` 将资源下载到工作区，再调用 `message(filePath)` 发送本地路径** [10, 23]。
*   **NotebookLM 连通性**：NotebookLM 必须依赖代理（优先 Surge 127.0.0.1:8234），限美国或台湾节点（香港节点不行），且必须开启增强模式让 Playwright 走代理 [14, 24, 25]。
*   **GitHub 分析防限流**：严禁直接通过浏览器访问 GitHub 网页，**必须通过 `git clone` 将代码拉取到本地沙盒后再进行分析** [14, 22]。

**5. 防卡死重试熔断机制**
*   **风险点**：在错误的方向上陷入机械重试的死循环 [16]。
*   **防范措施**：**如果同一个技术方案连续失败三次，必须强制跳出局部、切换思路或重新定义问题**，绝不要在错误方向上继续加码 [16]。

---

**报告路径**: /Users/lucifinil_chen/.openclaw/workspace/memory/notebooklm-daily-20260322.md
