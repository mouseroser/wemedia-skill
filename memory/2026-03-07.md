# 2026-03-07 Daily Log

## 14:17 - 配置清理
- 发现 `openclaw.json` 中有无效配置项 `messages.sendPolicy`
- 运行 `openclaw doctor --fix` 清理

## 16:26 - 再次配置清理
- 发现 `agents.defaults.tools` 无效配置项
- 运行 `openclaw doctor --fix` 清理
- 浏览器停止运行，已重新启动

## 踩坑记录
- ❌ `agents.defaults.tools` 不存在，会被 doctor 自动删除
- ✅ 工具配置应该在 agent 级别或通过 skills 管理，不在 defaults 里

## 16:39-17:21 - 群通知 bug 修复成功 🎉

### 问题
Sub-agent 在真实任务链路中无法发送消息到 Telegram 群（星链/星鉴/自媒体流水线）

### 问题演进历史（来自昨天）
1. **最初发现**（2026-03-06）：Sub-agent 在真实任务链路中无法发送消息
2. **错误方向 1**：以为是 agent 自推机制问题，改为 main 统一补发
3. **错误方向 2**：以为是通知链路设计问题，收口到 main
4. **错误方向 3**：以为是 agent 群可见性问题，要求 agent 自推
5. **方向切换**（2026-03-07）：重新定义问题为"工具权限"问题

### 根本原因
Sub-agent 默认没有 message 工具权限

### 解决方案
在 `openclaw.json` 中添加配置：
```json
{
  "tools": {
    "subagents": {
      "tools": {
        "alsoAllow": ["message"]
      }
    }
  }
}
```

### 关键教训
1. ✅ **优先查 docs.openclaw.ai 官方文档**，而不是只依赖 NotebookLM
2. ✅ **正确的配置路径是 `tools.subagents`**（顶层），不是 `agents.defaults.tools.subagents`
3. ✅ NotebookLM 的配置路径部分正确，但缺少了顶层 `tools` 这一层
4. ✅ 遇到 OpenClaw 配置问题时，查询优先级：docs.openclaw.ai → NotebookLM → config.schema
5. ✅ **同一问题若在同一方向上连续三次未解决，必须切换方向**（来自昨天的教训）

### 测试验证
- 测试 #4：gemini agent 成功发送消息到织梦群（-5264626153）
- 返回：`{"ok": true, "messageId": "15138", "chatId": "-5264626153"}`
- 配置通过 `openclaw doctor` 验证

### 相关历史问题（来自昨天）
1. **Telegram Privacy Mode 问题**：需要在 BotFather 关闭 + 重启 gateway + 重新添加 bot
2. **JSON 语法错误**：`requireMention: false` 应该是 `"requireMention": false`
3. **Agent 自推不可靠**：depth-1/depth-2 都可能丢消息，不能作为可靠通知路径
4. **通知链路纠偏**：agent 应该自推到职能群，main 负责监控群和补发

## 17:27-18:04 - OpenClaw 文档更新任务完整流程 📚

### 任务目标
更新 NotebookLM openclaw-docs notebook，包含 GitHub openclaw/openclaw 仓库的全部英文文档

### 遇到的问题和解决方案

#### 问题 1：文档来源错误（17:04）
**问题**：最初尝试从 docs.openclaw.ai 网站抓取文档
**解决**：应该从 GitHub openclaw/openclaw 仓库抓取
**教训**：回顾历史记录（2026-02-28），确认正确的文档来源

#### 问题 2：包含非文档内容（17:11）
**问题**：抓取了 809 个文件（5.8 MB），包含 .agent/、.github/ 等非文档目录
**现象**：
- 文件内容包含项目管理文件（如 update_clawdbot.md）
- 包含 GitHub 配置、工作流等无关内容
**解决**：只抓取 docs/ 目录的文件
**命令**：
```bash
find docs/ -name "*.md" -type f | sort
```

#### 问题 3：包含中文文档（17:27）
**问题**：抓取了 658 个文件（4.2 MB），包含 zh-CN、zh-TW 等非英文文档
**现象**：
- 文件中包含 `docs/zh-CN/` 目录的中文文档
- 包含多语言版本（pt-BR、de、es 等）
**解决**：使用 grep 排除所有非英文目录
**命令**：
```bash
find docs/ -name "*.md" -type f \
  | grep -v "/zh-CN/" \
  | grep -v "/zh-TW/" \
  | grep -v "/pt-BR/" \
  | grep -v "/de/" \
  | grep -v "/es/" \
  | grep -v "/ja/" \
  | grep -v "/ko/" \
  | grep -v "/fr/" \
  | sort
```
**结果**：351 个英文文件（2.4 MB）

#### 问题 4：NotebookLM 上传失败（17:38）
**问题**：2.4 MB 文件上传失败，返回 400 Bad Request
**错误信息**：
```
Error: Failed to get SOURCE_ID from registration response
RPC ADD_SOURCE failed
```
**尝试的方案**：
1. ❌ 直接上传文件（`--type file`）
2. ❌ 作为文本上传（`--type text`）
3. ❌ 分割文件（每个 2 MB）
**根本原因**：文件内容可能包含 NotebookLM 无法处理的格式或特殊字符
**最终解决**：去重处理后重新上传

#### 问题 5：去重处理（17:42）
**问题**：需要进一步减小文件大小
**分析**：检查 351 个文件是否有重复内容
**结果**：文档已经很好地组织，几乎没有重复
**处理**：保留所有 351 个文件（2.38 MB）
**上传**：✅ 成功上传到 NotebookLM
- Source ID: `019c81bf-c769-4c7e-977f-01205bbb26ea`
- 状态：Ready

### 最终结果

**文档统计**：
- 英文文件数：351
- 总行数：72,486
- 文件大小：2.38 MB
- 来源：github.com/openclaw/openclaw/docs/
- 语言：仅英文

**NotebookLM openclaw-docs notebook 当前状态**：
1. OpenClaw配置全量指南与参数结构图谱 ✅
2. config.schema ✅
3. openclaw_docs_dedup_20260307.md（新上传）✅

### 关键教训

1. **数据清洗很重要**：
   - 只抓取 docs/ 目录
   - 排除非英文目录（zh-CN、zh-TW、pt-BR、de、es、ja、ko、fr）
   - 排除非文档内容（.agent/、.github/、node_modules/ 等）

2. **NotebookLM 限制**：
   - 文件大小限制约 2-3 MB
   - 不支持直接更新已上传文件，只能删除+重新添加
   - 对文件内容格式有要求

3. **正确的文档来源**：
   - ✅ 从 GitHub openclaw/openclaw 仓库抓取
   - ❌ 不是从 docs.openclaw.ai 网站抓取

4. **迭代优化策略**：
   - 第一次：验证流程可行性
   - 第二次：清洗数据，排除无关内容
   - 第三次：进一步优化，确保上传成功

### 相关历史问题（来自昨天）
1. **NotebookLM 认证问题**：浏览器登录 ≠ CLI 认证，需要 `auth check --test` 验证
2. **notebooklm-py cookie 问题**：需要保留域名作用域，不能压平成普通 header
3. **openclaw-docs 被删除重建**：需要同步更新 `config/notebooks.json`
4. **NotebookLM 代理节点**：台湾和美国节点可用，香港节点不行

## 18:00 - 创建自动更新 Cron 任务 ⏰

### 任务配置
- **任务名称**：openclaw-docs-update
- **任务 ID**：772acaf3-b972-4f47-be34-c2ef655acf7d
- **执行频率**：每周日凌晨 04:00（Asia/Shanghai 时区）
- **执行者**：珊瑚（notebooklm agent）
- **模型**：minimax/MiniMax-M2.5（降本优化）
- **Thinking Level**：medium
- **超时**：1 小时

### 任务内容
1. 从 GitHub openclaw/openclaw 仓库抓取最新英文文档
2. 清洗数据（只保留 docs/ 目录，排除非英文内容）
3. 去重处理
4. 删除旧的 openclaw_docs 源
5. 上传新的文档到 NotebookLM
6. 推送结果到珊瑚群（-5202217379）和监控群（-5131273722）

### 模型选择策略
- **首次执行复杂任务**：使用 Opus（更强的推理和纠错能力）
- **定期重复任务**：使用 Minimax（流程已验证，降本 90%+）

### 下次运行时间
- 2026-03-09 04:00:00（本周日）

## 配置修复记录

### session.sendPolicy 配置
- 正确位置：`session.slicy`（不是 `messages.sendPolicy`）
- 配置内容：
  ```json
  {
    "session": {
      "sendPolicy": {
        "default": "deny",
        "rules": [
          {
            "action": "allow",
            "match": {
              "channel": "telegram"
            }
          },
          {
            "action": "allow",
            "match": {}
          }
        ]
      }
    }
  }
  ```

### tools.subagents 配置
- 正确位置：`tools.subagents.tools.alsoAllow`
- 配置内容：`["message"]`
- 作用：允许 sub-agent 调用 message 工具发送消息到 Telegram 群

## 今日成就 🎉

1. ✅ **修复了群通知 bug**：Sub-agent 现在可以正常发送消息到 Telegram 群
2. ✅ **更新了 Op档到 NotebookLM**：351 个英文文档，2.38 MB
3. ✅ **创建了自动更新 Cron 任务**：每周日自动更新，使用 Minimax 降本
4. ✅ **建立了完整的文档更新流程**：从 GitHub 抓取 → 清洗 → 去重 → 上传

## 核心经验总结

### 配置问题诊断
1. **优先查官方文档**：docs.openclaw.ai > NotebookLM > config.schema
2. **配置路径要准确**：顶层 vs agent 级别，字段名大小写
3. **使用 openclaw doctor 验证**：修改配置后立即验证

### 数据清洗策略
1. **明确排除规则**：使用 grep -v 排除不需要的目录
2. **验证清洗结果**：检查文件内容，确保没有遗漏
3. **控制文件大小**：NotebookLM 限制约 2-3 MB

### 模型选择策略
1. **复杂任务用 Opus**：首次执行、需要推理和纠错
2. **重复任务用 Minimax**：流程已验证、降本 90%+
3. **根据任务特点调整**：thinking level 也要相应调整

### NotebookLM 使用
1. **文件大小限制**：约 2-3 MB
2. **不支持原地更新**：只能删除+重新添加
3. **内容格式要求**：避免特殊字符和格式问题
4. **认证验证**：浏览器登录 ≠ CLI 认证，需要 `auth check --test`

### Sub-agent 工具权限
1. **显式授权**：在 `tools.subagents.tools.alsoAllow` 中配置
2. **测试验证**：spawn 后测试工具调用是否成功
3. **权限最小化**：只授权必要的工具

### 问题诊断方法论（来自昨天）
1. **同一方向连续三次失败必须换方向**
2. **重新定义问题**：从现象到根本原因
3. **查阅官方文档**：不要只依赖 AI 推理
4. **实测验证**：不要假设，要实际测试

## 完整问题清单

| # | 问题 | 根本原因 | 解决方案 | 状态 |
|---|------|---------|---------|------|
| 1 | Sub-agent 无法发送群消息 | 缺少 message 工具权限 | 配置 tools.subagents.tools.alsoAllow | ✅ 已解决 |
| 2 | 文档来源错误 | 误以为从网站抓取 | 从 GitHub 仓库抓取 | ✅ 已解决 |
| 3 | 包含非文档内容 | 抓取了整个仓库 | 只抓取 docs/ 目录 | ✅ 已解决 |
| 4 | 包含中文文档 | docs/ 下有多语言目录 | 使用 grep 排除非英文目录 | ✅ 已解决 |
| 5 | NotebookLM 上传失败 | 文件太大或格式问题 | 去重处理，减小文件大小 | ✅ 已解决 |
| 6 | NotebookLM 不支持更新 | API 限制 | 删除旧源+添加新源 | ✅ 已解决 |

## 可复用的解决方案

### 1. Sub-agent 工具权限配置
```json
{
  "tools": {
    "subagents": {
      "tools": {
        "alsoAllow": ["message"]
      }
    }
  }
}
```

### 2. 抓取 GitHub 仓库英文文档
```bash
git clone --depth 1 https://github.com/openclaw/openclaw.git /tmp/openclaw-docs
cd /tmp/openclaw-docs
find docs/ -name "*.md" -type f \
  | grep -v "/zh-CN/" \
  | grep -v "/zh-TW/" \
  | grep -v "/pt-BR/" \
  | grep -v "/de/" \
  | grep -v "/es/" \
  | grep -v "/ja/" \
  | grep -v "/ko/" \
  | grep -v "/fr/" \
  | sort > /tmp/docs_en_files.txt
```

### 3. 合并文档文件
```bash
output_file=~/output.md
cat > $output_file << 'EOF'
# Documentation

Generated: $(date)

---

EOF

while IFS= read -r file; do
  echo "" >> $output_file
  echo "---" >> $output_file
  echo "# File: $file" >> $output_file
  echo "" >> $output_file
  cat "$file" >> $output_file
  echo "" >> $output_file
done < /tmp/docs_en_files.txt
```

### 4. NotebookLM 源更新
```bash
# 删除旧源
old_source_id=$(nlm-gateway.sh source --agent main --subcmd list --notebook <name> --no-cache | jq -r '.data.sources[] | select(.title | contains("<keyword>")) | .id' | head -1)
if [ -n "$old_source_id" ]; then
  echo "y" | notebooklm source delete $old_source_id
fi

# 添加新源
notebooklm source add <file_path> --type text --title "<title>"
```

### 5. Cron 任务创建（定期更新）
```bash
openclaw cron add \
  --name <task_name> \
  --schedule "0 4 * * 0" \
  --agent <agent_id> \
  --model minimax/MiniMax-M2.5 \
  --thinking medium \
  --task "<task_description>"
```

## 昨天遗留的重要经验（2026-03-06）

### 架构和配置
1. **Workspace 架构统一**：Main `~/.openclaw/workspace/`，Sub-agents `~/.openclaw/workspace/agents/{id}/`
2. **Runtime/Workspace 分离**：Engine runtime 在 `~/.openclaw/agents/`，Workspace 在 `~/.openclaw/workspace/`
3. **智能体配置补全**：11 个智能体 × 8 件套 = 88 个配置文件

### 流水线规则
1. **主私聊会话禁止承载长编排**：避免占住主会话导致"像不可用"
2. **通知粒度随复杂度升级**：简单委派三段式，复杂编排按阶段通知
3. **Agent 自推到职能群**：main 负责监控群和补发，不替代 agent 可见性

### Telegram 配置
1. **Privacy Mode 必须关闭**：在 BotFather 中 `/setprivacy` → Disable
2. **修改后必须重启 gateway**：然后重新添加 bot 到群
3. **JSON 语法要正确**：对象键名需要引号

### NotebookLM 使用
1. **认证验证**：`auth check --test` 确认 CLI 认证状态
2. **代理节点**：台湾和美国可用，香港不行
3. **使用边界**：`openclaw-docs` 只用于 OpenClaw 相关问题
4. **notebook 重建**：需要同步更新 `config/notebooks.json`

### 问题诊断方法论
1. **连续三次失败必须换方向**：不要在错误方向上越走越远
2. **重新定义问题**：从现象到根本原因
3. **查阅官方文档**：优先级高于 AI 推理
4. **实测验证**：不要假设，要实际测试

## 19:23 - `intel/` 目录口径细化
- 确认 `intel/` 的原始定义不变：它是 Agent 间协作目录，遵守单写者原则
- 细化规则（最终版）：只要是多 agent 联合工作（流水线尤为明显），除了正式产物之外全部放到 `intel/collaboration/`
  - 正式产物：报告、审查结论、修复代码、文档交付等 → 各自 agent 目录
  - 非正式产物：外部 GitHub 项目、本地镜像、共享分析材料、临时脚本、调研素材等 → `intel/collaboration/`
- 单写者原则仍成立：由某一个 agent 负责 clone / 更新共享仓库，其他 agent 只读
- 已应用到当前架构：创建 `~/.openclaw/workspace/intel/collaboration/`，并将 `memory-lancedb-pro` 仓库移动到该目录下

## 21:47 - `memory_search` 修复完成
- 现象：`memory_search` 一直退化为 `provider: none`、`mode: fts-only`
- 排查结论：
  - 原来的 `gemini` embeddings 链不可用，原生 Gemini embeddings key 也无效
  - 当前聊天代理也不支持 `/embeddings`
  - 切到可用 embeddings provider 后，索引库仍然是 0 files / 0 chunks；需要显式执行一次强制建索引
- 已执行修复：
  - 将 `agents.defaults.memorySearch` 和 `main.memorySearch` 切到本地 `ollama`
  - 模型改为 `nomic-embed-text`
  - 本地拉取模型：`ollama pull nomic-embed-text`
  - 强制索引：`openclaw memory index --force --agent main`
  - 重启 gateway 生效
- 修复结果：
  - `openclaw status` 从 `vector unknown` 变为 `vector ready`
  - `openclaw memory status --json` 可见 main 已扫描 40 个 memory 文件并完成索引
  - `memory_search` 恢复为 `provider: ollama`、`mode: hybrid`
