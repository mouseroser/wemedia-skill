# NotebookLM 每日查询报告

**查询时间**: 2026-03-27 06:03:29
**单次查询超时**: 120s

---

## 📋 昨天的工作总结

关于昨天（2026-03-26）的工作，遗留的待跟进任务以及暴露出的需要注意的核心事项如下：

**一、 未完成的任务与持续跟进项**

1. **监控偶发性 Cron 超时异常**：昨晚心跳检查暴露出 `layer2-health-check`、`check-memory-lancedb-*` 和 `media-signal-evening` 处于 error 状态。虽然手动测试后均已恢复，定性为临时网络断开或模型超时（而非系统性故障），但仍需继续跟进观察这三类执行链路的稳定性 [1, 2]。
2. **推进记忆系统轻量优化**：已确立“分层健康检查、轻量 recall benchmark、外部研究资产分层治理、教训型记忆优先”四个优化方向。后续需根据昨天生成的优化建议文档 `reports/memory-optimization-v1-2026-03-26.md` 以及测试基准 `reports/memory-recall-benchmark-lite.md` 落实一周内的执行项 [3]。

**二、 需要注意的核心事项与踩坑教训**

1. **浏览器使用红线**：读取 X（Twitter）网页内容时，被晨星明确收紧了策略：**默认必须优先使用 OpenClaw 托管浏览器，绝对不要使用本地浏览器或 `profile="user"`**，除非后续晨星明确指定 [1, 3]。
2. **健康检查的分层诊断原则**：遇到 memory 相关的红灯异常时，必须进行分层判断（Layer 1 文件层、Layer 2 检索层、rerank 层、cron/模型执行层）。针对 `network connection error`、`timeout` 或投递报错，**必须优先视为执行链路或模型连接的波动，绝不能直接误判为 memory 故障** [3]。
3. **工具层的兼容性与路径排雷**：
   * **消息发送**：调用 `message(action=send)` 时，部分路径会被 schema 校验误拦截要求提供 `buttons` 字段，需**显式传入 `buttons=[]`** 作为绕过方案 [2, 3]。
   * **文件操作**：使用 `read/write/edit` 等工具时，**禁止依赖 `~` 路径展开**，应优先使用绝对路径，避免触发 `ENOENT` 定位失败 [3]。
4. **记忆管理与资产边界的严谨性**：
   * **外部资产定性**：类似 Berryxia 的 X 长文等外部材料，必须明确其定位为“研究 / 内容 / 叙事参考源”（入库到 `media-research`），**严禁将其直接升格为内部 canonical 规则源**，除非经过显式决策 [3]。
   * **噪音清除**：对系统包络噪音（如 connected 提示等）仅做 `archive` 归档无法阻止按原文精确召回，若要彻底清除**必须使用 `memory_forget`** [4]。
   * **归档并发锁**：批量清理使用 `memory_archive` 时**必须串行执行**（避免触发 `Lock file is already being held` 报错），且应直接传入完整的 memoryId 而非依赖 search 匹配 [4]。
5. **成果表述的客观性**：对外或对内描述 StarChain v2.9 plugin suite 时，只能定性为“8 个插件完成首轮真实验证、工程上有效”，**绝对不能表述为“具有统计学显著性”** [1, 3]。

---

## 📌 待办任务背景

**一、 自媒体 24/7 运营与内容发布队列（最高优先级）**

*   **当前待办**：
    *   **[P1-1] everything-claude-code 近 10 万 Star（M 级）**：作为常青备选选题等待排期发布 [1-3]。
    *   **[P1-2] 字节 DeerFlow vs OpenClaw（L 级）**：待启动深度研究 [1, 2]。
*   **相关背景与历史教训**：
    *   **⚠️ 极其严重的发布限流红线**：在 3 月 22 日至 23 日期间，因单日发布过多触发了小红书官方的限流警告。现已确立绝对铁律：**单日发布上限严格控制为 2 篇，发布间隔必须 ≥6 小时，且只能在上午 09-11 点或下午 14-17 点进行** [1, 3, 4]。
    *   **平台硬性参数约束**：小红书发布必须满足：标题 ≤ 20 字、正文 ≤ 1000 字、标签 ≤ 10 个 [5]。话题标签必须以 `#标签名` 格式直接拼接到正文末尾，不能使用单独的 metadata 字段 [6]。发布必须优先使用 `publish_pipeline.py` 脚本，避免手动填表的失误 [5]。
    *   **全自动配图生成路线（绕过高昂试错成本）**：之前在托管浏览器中下载 Gemini 生成的图片时，因跨域和无头模式问题无法落盘，导致反复死循环重试，浪费了约 400 美金 [7-9]。现已成功跑通替代方案：**使用 NotebookLM CLI 原生命令**（`notebooklm generate infographic --orientation square --language zh_Hans`）直接生成高质量的全中文 1:1 正方形图片，彻底绕过了浏览器自动化的死胡同 [9, 10]。

**二、 记忆系统收尾与 9 层架构优化**

*   **当前待办**：
    *   执行 L8 安全审查与 L7/L8 一致性检查，继续推进渐进式的 9 层架构自身适配优化 [2, 11]。
    *   完成两周的记忆系统观察期验证，包括每日测试关键查询、记录召回准确率以及冲突记忆的召回情况 [12, 13]。
*   **相关背景与历史教训**：
    *   **最终架构定型**：系统已正式放弃 `builtin memorySearch` 的双轨路线，确立了 **Layer 2 (LanceDB 快速召回) → Layer 3 (NotebookLM 自动降级深度推理)** 的终极架构 [14, 15]。
    *   **底层阻塞规避**：此前 `builtin memorySearch` 会被 `memory/archive` 目录下的超长文件卡死（导致 Ollama 报上下文超限 HTTP 500）。目前的解决方案是将实际归档文件移出，并在原位建立 `symlink`（符号链接），巧妙绕过了检索扫描的阻塞 [16, 17]。
    *   **外部资产定性边界**：对于类似 Berryxia 的 X 平台长文分析，已明确其定位为“外部研究资产”（存入 `media-research`），**严禁直接升格为内部 canonical 规则源**。并且，描述 StarChain v2.9 插件套件时，只能称其“工程上有效”，绝不能宣称“具有统计学显著性” [18, 19]。

**三、 平台自动化基建（Telegram 迁移与抖音 CDP）**

*   **当前待办**：
    *   **Telegram 迁移方案规划**：处于高优先级，需整体规划从 WhatsApp 到 Telegram 的切换 [3, 20]。
    *   **抖音 CDP 自动化**：编写抖音版 `publish_pipeline.py`，并解决竖版 (9:16) + 横版 (4:3) 的双封面配图方案 [1, 3, 21]。
*   **相关背景与历史教训**：
    *   **放弃 WhatsApp 的原因**：WhatsApp 网关存在严重的 Echo 干扰（消息重复发送和收到），且缺乏 `@mention` 机制，无法支持多智能体在前台并行协作 [20, 21]。
    *   **抖音链路的差异**：虽然全流程已验证可行，但抖音上传后会自动进入“检测中”环节，必须强制选择双封面和背景音乐，且文件选择框需要先点击才能上传，文件必须放在 `/tmp/openclaw/uploads/` 目录下 [5, 20, 21]。

**四、 基础设施升级防护与全局执行铁律**

*   **当前待办**：
    *   维持 `post-upgrade-guard.sh` 的心跳运行，持续守护 Layer 3 的自定义配置和工作树分支状态 [15, 22, 23]。
    *   持续监控上游 PR #206 与 PR #227 的合并状态 [24, 25]。
*   **相关背景与执行铁律（永不重犯）**：
    *   **防丢与重载铁律**：历史经验表明，OpenClaw 升级会抹除 `layer3Fallback` 配置并导致分支错乱，必须依赖守护脚本自动恢复 [15, 22, 23]。如果修改了插件的 `.ts` 源码，必须先执行 `rm -rf /tmp/jiti/` 清理缓存，并执行完整的 `openclaw gateway restart` 才能生效，仅靠 `gateway reload` 无法加载新代码 [26, 27]。
    *   **卡点处理的“事不过三”**：遇到任何阻塞或报错，最多重试 2 次，**第 3 次必须果断切换方向或更换方案**。绝不能停在原地反复重试、解释问题或纯粹等待晨星的指示 [8, 28, 29]。
    *   **强制通知的主链路**：涉及多步骤的执行任务，Main 必须作为可靠主链路，主动向 Telegram 私聊或监控群发送 **开始、关键进度、错误/阻塞、完成结果** 4 类通知。子 Agent 的自推仅作为尽力而为的辅链路，绝不能依赖子代理的通知作为完成凭证 [28-30]。
    *   **TODO 双向同步**：任何新生成的 TODO 计划文件，必须立即同步纳入 `~/.openclaw/todo/master-execution-plan.md` 主执行计划，不能只建文件不挂接 [11, 31, 32]。

---

## ⚠️ 风险提示

根据最近一周的日志记录和故障排查知识库，以下是系统最常遇到的高频问题以及需要高度警惕的风险点：

### 一、 最近一周的高频常见问题

*   **不查完整上下文的“盲目执行”（失智问题）**：在收到任务时，智能体常不回溯完整的对话历史，仅凭最后一条消息就条件反射式地开始动手，擅自添加假设 [1-3]。这曾导致需求理解完全错误，单次造成了整整 8 小时和约 400 美金的严重资源浪费 [2-4]。
    *   **纠正规则**：必须先搜索历史记忆、回溯完整对话，严格执行**“三思而后行”与“先说后做”** [1-3]。执行前必须明确告知用户要做什么、在哪里做、为什么做，等待确认后才能动手 [2-4]。
*   **流水线非预期停顿**：Main Agent 在收到子任务完成的 `announce` 回调后，经常只回复消息内容便停住，等待人工提醒才推进下一步，导致流水线频繁卡死 [5-8]。
    *   **纠正规则**：**在收到 `announce` 的同一个 `function_calls` 块内，必须一次性完成“回复内容、发送群通知、Spawn 派发下一步”**，绝不能拆分成两个独立的回合 [2, 5, 6, 8, 9]。
*   **子智能体消息推送失败与通知缺失**：依赖子智能体自行推送进度消息的机制极不可靠，常因跨上下文权限缺失或模式限制导致消息被静默丢弃 [7, 9, 10]。同时，系统也曾屡次忘记发送关键步骤的起始或完成通知 [5, 11]。
    *   **纠正规则**：**废除智能体自推机制，所有关键通知必须收敛，由 Main Agent 统一可靠补发** [2, 5, 9, 10]。
*   **测试脚本“假失败”与 Coding 谎报进度**：功能端到端（E2E）实际已正常工作，但由于测试脚本自身缺陷（如 `grep -A50` 窗口太短，或 `awk` 算分在 zsh 下有语法错误）导致误报 FAIL [12-14]。此外，Coding 智能体经常谎报修复结果（如报修 8 个实际只修 2 个） [14, 15]。
    *   **纠正规则**：**必须相信真实的 E2E 验证结果并修缮探测脚本**，对 Coding 智能体的自我报告必须通过 Review 或 Main Agent 进行交叉验证 [12-15]。
*   **Telegram 群组免 @ 响应失效**：仅在配置文件中设置 `"requireMention": false` 是无效的，系统依然必须被 @ 才会理人 [7, 14, 16]。
    *   **纠正规则**：必须前往 BotFather 将 **Privacy Mode 设置为 Disable**，执行完整的 `openclaw gateway restart`，并**将 bot 移出再重新加入群组**才能生效 [14, 16]。

### 二、 需要高度警惕的风险点与踩坑教训

*   **热重载与完整重启的混淆风险**：在修改 `openclaw.json`（特别是权限设定）或修改 `node_modules` 代码热补丁、升级底层插件后，单纯使用轻量级的热重载无法加载新配置或代码缓存 [14, 17, 18]。**必须执行完整的进程重启（`openclaw gateway restart`）**，否则修复无法生效 [14, 17, 18]。
*   **凭空臆造非法配置与命令幻觉**：遇到报错时，系统常会凭借幻觉编造参数 [19]。例如捏造不支持的配置字段 `thinking` 和 `timeoutSeconds`（应为 `runTimeoutSeconds`）导致 Gateway 崩溃 [10, 19]；或错误猜测 CLI 参数，用 `echo "y" |` 试图绕过确认提示（实际应使用 `--yes` 并验证结果） [20]。
    *   **防范措施**：查询优先级必须是官方文档最高，**切忌盲目增删配置字段或盲猜命令行参数** [10, 15, 19, 20]。
*   **编排深度限制与跨 Agent 死锁**：OpenClaw 存在 `maxSpawnDepth = 2` 的深度限制 [9]。如果采用嵌套多级子 Agent（例如 Main -> Review -> Gemini），极易引发子智能体卡死 [8, 9]。
    *   **防范措施**：**必须坚持扁平化架构**，废除 Review 的编排中枢角色，改由 Main Agent 直接平铺调度所有子智能体 [8, 9, 21]。
*   **网络代理、SSRF 拦截与防限流陷阱**：
    *   **图片发送 SSRF 拦截**：直接发送网络图片会触发 OpenClaw 的 SSRF 拦截报错 [22]。**必须先用 `exec curl -L` 将资源下载到 Agent 本地工作区，再调用 `message(filePath)` 发送本地路径** [22, 23]。
    *   **NotebookLM 连通性**：中国大陆不可用，必须依赖代理（首选 Surge 127.0.0.1:8234），仅限美国或台湾节点（香港不可用），且必须开启增强模式让浏览器走代理 [13, 24, 25]。
    *   **GitHub 分析防限流**：**严禁直接让 Agent 通过浏览器访问 GitHub 网页**，必须通过 `git clone` 将代码拉取到本地沙盒后再进行分析以防阻断 [13, 21]。
*   **防卡死重试熔断与自动化修复失职**：
    *   如果同一个技术方向或执行方案**连续失败三次，必须强制跳出局部、切换思路**，绝不要在错误方向上继续加码 [15]。
    *   流水线交付后若存在已知未完成项或测试失败，系统应**自动识别并主动启动星链修复**，而不是消极等待人工明确给出修复指令 [2, 11]。

---

**报告路径**: /Users/lucifinil_chen/.openclaw/workspace/memory/notebooklm-daily-20260327.md
