## 自媒体运营日结 — 2026-03-24 (周二) 06:21

> cron: media-daily-retro | 执行人：main (小光)

### 今日状态说明

今日日结于 06:21 执行，DAILY-PUBLISH-PLAN 尚未生成（计划 cron 通常 08:00 生成），引用昨日（2026-03-23）最终数据。

---

### 昨日（03-23）最终发布总结

| # | 标题 | 平台 | 状态 | 发布时间 |
|---|------|------|------|----------|
| 1 | 「Google出手，AI编程变天了」| 小红书 | ✅ 已发 | 07:52 |
| 2 | 「OpenClaw曝漏洞，3个月用户慌吗？」| 小红书 | ✅ 已发 | 08:24 |

**产出：2篇（符合当日2篇上限策略）**

---

### Hot Queue 当前状态（03-24 早晨）

| 条目 | Level | 状态 |
|------|-------|------|
| [P1-1] everything-claude-code 近 10 万 Star | M | ⏳ 本周排期（今日可优先考虑）|
| [P1-2] 字节 DeerFlow vs OpenClaw | L | ⏳ 待启动研究 |
| [P2-1] Flash-MoE 397B | - | ⏳ 暂缓，需晨星实测 |

---

### 关键复盘与学习（03-23 → 03-24 传承）

**已固化策略（勿回退）：**
- 小红书每日 2 篇上限，间隔 ≥6 小时
- 发布时段：上午 09-11 + 下午 14-17
- 大厂+冲突感标题 = 流量密码（03-20 数据验证）

**未完成待跟进：**
1. 抖音版 publish_pipeline.py（脚本已有基础，需完善）
2. Telegram 迁移方案（高优先级）
3. DeerFlow 研究启动
4. OpenClaw v2026.3.22 升级适配（等待晨星确认时机）

---

### 今日内容规划方向（待今日 DAILY-PLAN 生成确认）

- **首选**：[P1-1] everything-claude-code（M 级，30min，时效宽）
- **备选**：根据早晨热点扫描结果临时加入
- **不超过 2 篇**

---
- 2026-03-24T00:23:11.790Z [other:agent:main] agent=main source=auto-capture 标题要更抓眼球，控制在20字内 必须加配图 标签数量不够，注意不要多于10个
- 2026-03-24T01:09:42.270Z [other:agent:main] agent=main source=auto-capture 标题要更抓眼球，控制在20字内 必须加配图 标签数量不够，注意不要多于10个

## 2026-03-24 午后日结补充（11:42）

### 今日发布状态

**实际发布：1 篇**
- [P0-2] Claude Code Channels vs OpenClaw 对比（小红书，早间）

### 明日重点

**P0-2 必须推进** — Claude Code Channels 话题已发布约 38h，窗口持续收窄。明日晨星一确认立即启动 S 级流水线出稿。P1-3 窗口剩余约 6-12h，若明日晨间无新热点可作为第二条补发。

### 关键教训

- 今天暴露的问题：即使 pipeline 已就绪、计划已生成，没有晨星确认信号就不会动。以后早间确认请求要更早发出，给流水线留足执行时间。

## 2026-03-24 午前关键沉淀（耐久记忆）

### 1) 抖音发布链路 smoke test 跑通
- 全链路验证成功：视频上传 → 标题填写 → 仅自己可见 → 真正发布。
- 关键限制：`browser(action=upload)` 的文件必须放在 `/tmp/openclaw/uploads/`，跨目录文件需先复制过去。
- 经验：OpenClaw Browser 已登录态可直接操作创作者中心；独立 Playwright 无法稳定复用 httpOnly cookie。

### 2) 小红书正式发布必须优先用 publish_pipeline.py
- 本次内容《Claude Code 手机控制上线了》最终通过 `~/.openclaw/skills/xiaohongshu/scripts/publish_pipeline.py` 成功发布。
- 先手动 CDP 填表容易把正文填进标题栏，最终还是 pipeline 一次跑通上传/标题/正文/标签/发布/成功页校验。
- 本次验证出的硬约束：
  - 标题 ≤ 20 字
  - 正文 ≤ 1000 字
  - 标签 ≤ 10 个
- 结论：以后小红书发布，默认先走 pipeline，不要先手动浏览器摸索。

### 3) NotebookLM 生图规则再次确认
- 生成配图时，禁止直接用共用 notebook（如 `openclaw-docs` / `media-research`）生图。
- 正确流程：创建临时 notebook → 加入单一干净 source → 生成 infographic → 下载 → 删除临时 notebook。
- 今天重新按该规则完成了一次临时 notebook 生图流程。

### 4) OpenClaw 已升级到 v2026.3.23-1
- 升级方式：按方案 A，仅升级主程序，不同时改架构/插件逻辑。
- 第一轮回归通过项：browser attach/open/snapshot、memory_stats、memory_list、post-upgrade-guard、runtime plugin path、rerank sidecar。
- 升级后仍需关注：runtime plugin worktree branch 仍是 `adapt/beta9-selective-20260323`，不是标准 `runtime/layer3-fallback-active`，需人工审计。

### 5) openclaw-weixin 升级后兼容性问题
- 升级后暴露出 `openclaw-weixin` 扩展与新版 plugin-sdk 的兼容问题：旧聚合导入失效，`resolvePreferredOpenClawTmpDir` 等符号不再从旧入口可用。
- 已做最小兼容补丁：
  - 给扩展补 `node_modules/openclaw` symlink，恢复 `openclaw/*` 路径解析
  - 将部分导入改到新版细分模块（如 `core` / `infra-runtime` / `account-resolution` / `text-runtime` / `channel-runtime`）
- 结果：`openclaw status` 已恢复正常，不再被该扩展阻塞；但扩展内部仍有 TS/ESM `.js` 子模块引用历史债，后续可继续收口。

## 2026-03-24 下午收尾（Skill / 目录 / GitHub）

### 1) 自媒体 skill 架构完成统一
- `wemedia` 明确为全平台运营编排层。
- `xiaohongshu`（原 `media-tools`）明确为小红书执行工具。
- `douyin` 明确为抖音执行工具，并接入 wemedia 的 Step 7.5 发布节点。
- 同步完成：`wemedia SKILL.md`、`douyin SKILL.md`、`xiaohongshu SKILL.md`、`ops-routing-v2.md`、`platform-douyin-v1.md`、流程图、`AGENTS.md`、`agents/wemedia/AGENTS.md`。

### 2) 流水线实测通过
- 小红书 pipeline dry-run 成功：Chrome 连接 → 登录态确认 → 上传图片 → 填标题正文 → `READY_TO_PUBLISH`。
- 抖音 pipeline 验证成功：修复 OpenClaw Browser `browser.extraArgs=["--remote-allow-origins=*"]` 后，`publish_douyin.py --step open_page` 成功打开「抖音创作者中心」。
- 结论：`wemedia` / `xiaohongshu` / `douyin` 三个 skill 当前可正常工作。

### 3) `intel/` 目录完成清理
- 删除了除核心目录外的大部分旧残留与测试产物。
- 当前建议保留的核心结构：
  - `intel/media-ops/`
  - `intel/collaboration/media/`
  - `intel/starchain-templates/`
- 额外收尾：全局扫描确认工作区已无 `intel/media-tools` 的有效引用残留（仅 session 历史记录中保留旧字样）。

### 4) 三个 skill 已推送到 GitHub
- `mouseroser/wemedia-skill`：已更新。
- `mouseroser/media-tools-skill`：推送后已改名为 `mouseroser/xiaohongshu-skill`，并更新本地 remote。
- `mouseroser/douyin-skill`：新建 public 仓库并完成首推。

## 自媒体日结 — 2026-03-24 晚间汇总（21:30）

> cron: media-daily-retro | 执行人：main (小光)

**今日发布：1 篇**
- ✅ [P0-2]「Claude Code 手机控制上线了 vs OpenClaw」— 小红书，早间发布

**打回 / 跳过：**
- ⏳ [P1-3] OpenClaw 安全修复体验 — 窗口收窄（距发布已 36h+），今日未执行，时效即将耗尽
- ⏳ [P1-1] everything-claude-code 10 万 Star — 待排期，明日备选
- ⏳ [P1-2] 字节 DeerFlow vs OpenClaw — L 级，本周启动研究

**明日重点：** everything-claude-code 近 10 万 Star（M 级，常青，时效宽），晨星早间确认后立即启动；P1-3 若明日晨间晨星有空可作第二条补发（时效最后窗口）。

---

### 5) 新坑 / 教训
- macOS 默认没有 `timeout` 命令，脚本化长任务不要直接依赖 GNU timeout。
- `openclaw browser start` 不能像普通 Chrome 那样直接追加 CLI 参数；要通过 gateway config 的 `browser.extraArgs` 配置并重启 browser 生效。
- `gateway config.patch` 的 `raw` 需要传 object，不是数组或裸值。
- 2026-03-24T07:17:38.285Z [decision:agent:main] agent=main source=memory_store 2026-03-24：自媒体 skill 架构完成统一。wemedia = 全平台运营编排层；xiaohongshu（原 media-tools）= 小红书执行工具；douyin = 抖音执行工具，并接入 wemedia Step 7.5 发布节点。同步更新了 SKILL.md、ops-routing-v2、platform-douyin-v1、流程图、AGENTS.md，并验证小红书/抖音两条流水线可用。
- 2026-03-24T13:45:10.584Z [other:agent:main] agent=main source=auto-capture 小红书流水线遇到的问题和解决方案你记住了吗？

---

## 2026-03-24 晚间总结（21:50 CST）

### 今日自媒体发布
| # | 标题 | 平台 | 状态 | 发布时间 |
|---|------|------|------|----------|
| 1 | Claude Code Channels vs OpenClaw 对比 | 小红书 | ✅ | 早间 |
| 2 | AI正在裸奔，OpenClaw漏洞实录 | 小红书 | ✅ | 21:39 |

### P1-3 OpenClaw 安全争议全景梳理 M 级流水线完成
- Step 1.5：gemini agent 搜索三线（HN漏洞→批评→官方修复）✅
- Step 3：wemedia subagent 创作初稿（标题/正文/配图）✅
- Step 7：晨星确认 + 标题修订（23字→压缩到18字）✅
- 发布：xiaohongshu CDP ✅

### HEARTBEAT.md 检查路径已更新
- `intel/xiaohongshu/` → `intel/media-ops/`

### cron 模型调整（今日完成）
- `media-signal-noon`：gemini → Sonnet（解决 thought_signature 400）
- `media-signal-evening`：Sonnet + 300s timeout

### 今日新坑记录
1. gemini agent html2text 缺失（已安装）
2. gemini 模型 allowlist：统一用 `-preview`
3. 小红书标题逐字计数导致超限
4. media-signal crons 超时问题（prompt 任务量 vs cron timeout）
