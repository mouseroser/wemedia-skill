# 2026-03-16

## 00:00 记忆系统遗留问题收尾

- 【PR #187 收口】确认 `CortexReach/memory-lancedb-pro` 的 PR #187 挂在过时的 `main-legacy` base 上，diff 已经脏掉且处于 `CONFLICTING`。已基于当前 upstream `master` 重建一个干净 PR：#227 `fix: default agentId to 'main' in memory_list and memory_stats`。随后关闭旧的 #187，避免 maintainer 在大 diff 上浪费时间。
- 【PR #206 状态确认】`feat/layer3-notebooklm-fallback` 当前仍为 `OPEN + MERGEABLE`，checks 状态为 `version-sync ✅`、`claude-review skipped ✅`、`cli-smoke` 正常跑；不再重复刷评论，改为把 cron 监控从“#206 + #210”更新为“#206 + #227”。
- 【记忆压缩超时问题】确认 `记忆压缩检查` cron 已切到 `anthropic/claude-sonnet-4-6`，最近一次运行状态 `ok`，delivery 正常，视为已解决。
- 【auto-capture 噪音清理】在 Layer 2 中识别并删除 5 条明显系统包络噪音记忆（`System: [...]` / `Conversation info` / `Sender (untrusted metadata)`）。删除后需执行一次完整 `openclaw gateway restart`，SIGUSR1 热重载不会刷新插件持有的 LanceDB snapshot。
- 【噪音审计补强】为 `memory-quality-audit.sh` 增加 Layer 2 噪音检查：统计 `system_events` / `conversation_info` / `sender_metadata` 三类噪音条目。2026-03-16 00:36 首次验证结果为 0 条。

## 教训

1. 旧 PR 如果 base 指向过时分支（如 `main-legacy`），继续修会越来越脏，最好的处理方式通常是：基于当前 upstream 默认分支重建一个干净 PR，再关闭旧 PR。
2. LanceDB 底层删除记录后，OpenClaw 插件在热重载下可能仍保留旧 snapshot；要让检索结果真正刷新，必须完整 `openclaw gateway restart`。
3. auto-capture 噪音不能只靠事后手工删除，必须在审计链路里显式检查 `System:` / `Conversation info` 这类系统包络文本。
- 2026-03-16T02:30:50.157Z [decision:agent:main] agent=main source=auto-capture System: [2026-03-16 10:29:58 GMT+8] Model switched to Opus (anthropic/claude-opus-4-6).  Conversation info (untrusted metadata): ```json {   "message_id": "23215",   "sender_id": "1099011886",   "sender": "快樂小豬 🐷",   "timestamp": "Mon 2026-03-16 10:30 GMT+8" } ```  Sender (untrusted metadata): ```json {   "label": "快樂小豬 🐷 (1099011886)",   "id": "1099011886",   "name": "快樂小豬 🐷",   "username": "Lucifinil_Chen" } ```  但是所有agent的信息都没有啊
- 2026-03-16T02:32:25.062Z [decision:agent:main] agent=main source=auto-capture System: [2026-03-16 10:29:58 GMT+8] Model switched to Opus (anthropic/claude-opus-4-6).  Conversation info (untrusted metadata): ```json {   "message_id": "23215",   "sender_id": "1099011886",   "sender": "快樂小豬 🐷",   "timestamp": "Mon 2026-03-16 10:30 GMT+8" } ```  Sender (untrusted metadata): ```json {   "label": "快樂小豬 🐷 (1099011886)",   "id": "1099011886",   "name": "快樂小豬 🐷",   "username": "Lucifinil_Chen" } ```  但是所有agent的信息都没有啊
- 2026-03-16T02:36:48.917Z [fact:agent:main] agent=main source=auto-capture 还有，控制台的信息没有问题，但是各agent的信息还是没有显示，你一直没有修复吗？
- 2026-03-16T02:39:02.750Z [fact:agent:main] agent=main source=auto-capture 还有，控制台的信息没有问题，但是各agent的信息还是没有显示，你一直没有修复吗？
- 2026-03-16T02:42:09.447Z [decision:agent:main] agent=main source=memory_store 【星鉴/星链流水线通知缺陷】2026-03-16：isolated session 代做各 agent 工作时，各 agent 的职能群完全没有消息（织梦群、小曼群、珊瑚群等）。原因：没有真正 spawn 各 agent（gemini/openai/notebooklm/claude），而是 isolated session 自己写文件。修复方向：(1) 必须真正 spawn 各 agent，不能代做；(2) main 补发通知时不仅发监控群，也要发对应的职能群。晨星在织梦群看不到任何任务信息，指出了这个问题。
- 2026-03-16T02:54:31.235Z [other:agent:main] agent=main source=auto-capture 不用，你应该启动星鉴将这个情况加入重新评估方案，记住这次不要再用solated session了
- 2026-03-16T02:58:33.982Z [other:agent:main] agent=main source=auto-capture 不用，你应该启动星鉴将这个情况加入重新评估方案，记住这次不要再用solated session了
- 2026-03-16T03:22:05.559Z [decision:agent:main] agent=main source=memory_store 【星鉴流水线 v2.0 完成】2026-03-16：小红书多 Agent 运营方案 v2.0 已交付确认。核心内容：(1) 4 Agent 架构（radar/writer/social/analyst）+ xiaohongshu-mcp 只读集成；(2) 方案 C+：只读允许/写入禁止，MCPorter 接入；(3) xiaohongshu-mcp 是非官方方案（浏览器模拟），非小红书官方 MCP；(4) 写入功能设渐进式开放路径。本次流水线执行方式正确：main 在主会话直接 spawn 各 agent（gemini→openai→notebooklm→claude→gemini→docs），各职能群均收到通知，spawn 后立即补发"开始"通知。报告路径：agents/docs/final-report-xiaohongshu-v2-20260316.md
- 2026-03-16T03:44:17.942Z [decision:agent:main] agent=main source=memory_store 【禁止 isolated session 编排 — 星链 + 星鉴统一决策】2026-03-16 晨星确认：所有流水线（星链 v2.8、星鉴 v2.0）禁止使用 isolated session 编排（sessions_spawn agentId="main"）。正确做法：Main 在主会话中直接逐步 spawn 各 agent（gemini/openai/notebooklm/claude/coding/test/docs）。原因：isolated session 中的 agent 没有 Telegram 群绑定，职能群收不到消息。已更新：starchain SKILL.md、stareval SKILL.md、AGENTS.md。
- 2026-03-16T11:31:03.389Z [decision:agent:main] agent=main source=auto-capture System: [2026-03-16 12:15:54 GMT+8] Model switched to Minimax (minimax/MiniMax-M2.5).  Conversation info (untrusted metadata): ```json {   "message_id": "23344",   "sender_id": "1099011886",   "sender": "快樂小豬 🐷",   "timestamp": "Mon 2026-03-16 19:29 GMT+8" } ```  Sender (untrusted metadata): ```json {   "label": "快樂小豬 🐷 (1099011886)",   "id": "1099011886",   "name": "快樂小豬 🐷",   "username": "Lucifinil_Chen" } ```  记忆系统工作状态
- 2026-03-16T11:52:00.478Z [decision:agent:main] agent=main source=auto-capture System: [2026-03-16 19:50:15 GMT+8] Model switched to GPT (openai/gpt-5.4).  Conversation info (untrusted metadata): ```json {   "message_id": "23363",   "sender_id": "1099011886",   "sender": "快樂小豬 🐷",   "timestamp": "Mon 2026-03-16 19:51 GMT+8" } ```  Sender (untrusted metadata): ```json {   "label": "快樂小豬 🐷 (1099011886)",   "id": "1099011886",   "name": "快樂小豬 🐷",   "username": "Lucifinil_Chen" } ```  检查一下调整的timeoutSeconds合理吗
- 2026-03-16T12:06:54.460Z [fact:agent:main] agent=main source=memory_store 【修复 layer3-deep-insights 失败根因】2026-03-16：layer3-deep-insights 的问题不是 timeout，而是三件事叠加：(1) 脚本仍引用旧 Memory Archive notebook ID `bb3121a4...`，应改为当前 `94f8f2c3...`；(2) 脚本错误使用不存在的 `notebooklm query` 命令，正确命令是 `notebooklm ask -n <notebook-id> "..."`；(3) cron payload 对监控群推送描述不够明确，导致某次 `message(action="send")` 未带 target，报 `Action send requires a target`。已修复脚本、文档和 cron prompt。

## 20:36 Layer 3 深度洞察修复验证

- 【layer3-deep-insights 成功跑通】2026-03-16 晚间对 `~/.openclaw/skills/memory-architecture-manager/scripts/layer3-deep-insights.sh` 进行了真实执行验证。修复后脚本成功生成 `memory/layer3-insights-20260316.md`，最终文件约 20.9KB，进程 `exit code 0`。相比旧版只有 5KB 且大量失败占位，说明 Layer 3 查询链路已恢复可用。
- 【脚本加固】为避免 NotebookLM 单次请求长时间卡住，新增 `run_notebook_query()` Python wrapper：内部调用 `notebooklm ask -n <notebook-id> <question>`，并对每个查询设置 90 秒超时；若超时则保留已产出的部分内容并追加 `（查询超时）`，避免整份报告挂死。
- 【当前运行表现】本次 8 个查询中 6 个成功、2 个超时，但脚本整体仍能完成并输出完整报告。结论：剩余风险主要是 NotebookLM API 偶发响应慢，不再是 notebook ID、CLI 命令或脚本死锁问题。
- 【关键教训】NotebookLM CLI 在这台机器上正确命令是 `notebooklm ask`，不是 `notebooklm query`；遇到 CLI 不确定时必须先跑 `notebooklm --help` / `notebooklm ask --help` 验证。对于可能慢或不稳定的外部 CLI，shell 脚本必须显式加超时与降级输出，不能直接裸跑串行多次调用。
- 2026-03-16T13:12:32.895Z [decision:agent:main] agent=main source=memory_store 【layer3-deep-insights 全面改用 nlm-gateway】2026-03-16：layer3-deep-insights.sh 所有 NotebookLM 查询已从裸 `notebooklm ask -n` 改为走 `nlm-gateway.sh query --agent main --notebook memory-archive`。好处：(1) 自动走 ACL / 缓存 / 重试 / 速率限制；(2) 返回结构化 JSON，不再需要 Python wrapper 做超时保护；(3) source 统计也改用 gateway source list（返回 .data.count）。修复后验证：8 问全部成功，0 查询失败/超时，报告 29KB，exit code 0。此前的错误：直接调用裸 CLI 无保护、用不存在的 `notebooklm query` 命令、旧 notebook ID。
- 2026-03-16T13:39:23.030Z [decision:agent:main] agent=main source=memory_store 【NotebookLM cron 脚本全面修复】2026-03-16：5 个脚本全部修复并验证通过。  1. **sync-memory-to-notebooklm.sh**（最重要）：    - 问题：裸 CLI + 先删光所有 source 再重传 + 文件选择太宽（所有 2026-*.md 而非仅 daily 主日志）→ 85 个 source 全量同步超时    - 修复：(a) 从 notebooks.json 动态解析 ID；(b) 只选 `YYYY-MM-DD.md` 格式主日志（不再匹配子文件）；(c) 增量同步（diff-based：对比当前 source 列表 vs 目标文件集，只删多余、只传缺失，不全删全传）    - 验证：86→9 个 source（删 77 多余、0 新增），exit 0  2. **daily-query.sh**：    - 问题：原来 gateway 返回的 JSON 被原样写入报告    - 修复：Python 解析 JSON 提取 `.data.answer`    - 验证：报告输出人类可读内容  3. **layer3-mont

## NotebookLM Cron 脚本全面修复（21:26-21:40）

晨星要求检查所有 cron 是否有 layer3-deep-insights 同类问题，然后执行修复。

### 修复清单

#### 1. sync-memory-to-notebooklm.sh（最重要，timeout 根因）
- **问题**：先删光所有 source 再全量重传 + 选文件范围太宽（所有 `2026-*.md`）→ 85 个 source 同步超时
- **修复**：
  - notebooks.json 动态解析 notebook ID
  - 只选 `YYYY-MM-DD.md` 格式 daily 主日志（不匹配细分日志）
  - diff-based 增量同步（对比现有 vs 目标，只删多余只传缺失）
- **验证**：86→9 个 source（删 77 多余、0 新增），exit 0

#### 2. daily-query.sh（报告质量问题）
- **问题**：gateway 返回的整段 JSON 原样写入报告
- **修复**：Python 解析 JSON 提取 `.data.answer`
- **验证**：3 个查询全部成功，报告人类可读

#### 3. layer3-monthly-archive.sh（确定 4/1 会炸）
- **问题**：`notebooklm source upload`（不存在命令）+ `--source`（不存在选项）+ `--notebook`（应为 `-n`）
- **修复**：改为正确 CLI 语法 + notebooks.json 动态解析 + 管道改文件解析避免 SIGPIPE
- **验证**：exit 0

#### 4. update-openclaw-docs.sh（硬编码 ID）
- **修复**：从 notebooks.json 动态解析 openclaw-docs notebook ID，fallback 硬编码

#### 5. layer3-deep-insights.sh（之前修的补丁）
- 裸 CLI fallback 中 `--notebook` 统一改为 `-n`
- 2026-03-16T23:24:46.149Z [decision:agent:main] agent=main source=auto-capture System: [2026-03-17 07:23:07 GMT+8] Model switched to Minimax (minimax/MiniMax-M2.5).  Conversation info (untrusted metadata): ```json {   "message_id": "23456",   "sender_id": "1099011886",   "sender": "快樂小豬 🐷",   "timestamp": "Tue 2026-03-17 07:24 GMT+8" } ```  Sender (untrusted metadata): ```json {   "label": "快樂小豬 🐷 (1099011886)",   "id": "1099011886",   "name": "快樂小豬 🐷",   "username": "Lucifinil_Chen" } ```  按优先级列出todo待办任务
- 2026-03-16T23:35:01.024Z [fact:agent:main] agent=main source=memory_store 【记忆观察期 Day 3】2026-03-17：完成 15 条 benchmark。Layer 2 可用命中 12/15，平均质量 4.1/5；长期/规则型知识稳定，但“40k tokens 阈值”“刚才新建的 TODO 文件名”“当前策略”等短期/精确策略型问题仍弱。Layer 3 在 5/15 查询中被动触发，但 3 次因 notebooklm session file locked + gateway timeout 显式失败，另外 2 次未提供有效补充。
- 2026-03-16T23:35:47.446Z [decision:agent:main] agent=main source=auto-capture System: [2026-03-17 07:26:14 GMT+8] Model switched to GPT (openai/gpt-5.4).  Conversation info (untrusted metadata): ```json {   "message_id": "23464",   "sender_id": "1099011886",   "sender": "快樂小豬 🐷",   "timestamp": "Tue 2026-03-17 07:26 GMT+8" } ```  Sender (untrusted metadata): ```json {   "label": "快樂小豬 🐷 (1099011886)",   "id": "1099011886",   "name": "快樂小豬 🐷",   "username": "Lucifinil_Chen" } ```  完成P0
- 2026-03-16T23:37:05.006Z [decision:agent:main] agent=main source=auto-capture System: [2026-03-17 07:23:07 GMT+8] Model switched to Minimax (minimax/MiniMax-M2.5).  Conversation info (untrusted metadata): ```json {   "message_id": "23456",   "sender_id": "1099011886",   "sender": "快樂小豬 🐷",   "timestamp": "Tue 2026-03-17 07:24 GMT+8" } ```  Sender (untrusted metadata): ```json {   "label": "快樂小豬 🐷 (1099011886)",   "id": "1099011886",   "name": "快樂小豬 🐷",   "username": "Lucifinil_Chen" } ```  按优先级列出todo待办任务
- 2026-03-16T23:57:44.941Z [decision:agent:main] agent=main source=memory_store 【修复 Layer 3 fallback 连续 3 次 session lock】2026-03-17：memory-lancedb-pro 的 `runNotebookLMFallbackQuery()` 因未给 `openclaw agent --agent notebooklm` 显式传 `--session-id`，导致多次 fallback 复用同一个 NotebookLM session 文件并触发 `.jsonl.lock` 争用。已改为每次 fallback 生成唯一 `memory-layer3-fallback-*` session id，新增回归测试，commit=567a59c561494e91e92f60da890839fe027d5be9。运行态按规则清 `/tmp/jiti` 后重启 gateway 生效。
