# NotebookLM 每日查询报告

**查询时间**: 2026-03-28 06:04:28
**单次查询超时**: 120s

---

## 📋 昨天的工作总结

关于昨天（2026-03-27）的工作，未完成的任务排期以及暴露出的核心执行纪律与注意事项如下：

**一、 未完成的任务与后续排期**

1. **自媒体内容队列暂缓与排期**：
   * **[P0-4] LiteLLM 供应链攻击排查**：昨日未启动，时效余温已过，建议并入本周安全三部曲 [1]。
   * **[P1-8] Skill 化趋势**：为优先发布 DeerFlow 已顺延，将作为今日的首条候选稿件 [1]。
   * **[P1-1] everything-claude-code 与 [P1-4] AI 编程辩论**：作为常青选题，已明确规划在周六/周日执行 [1]。
2. **NotebookLM 生图链路故障待修**：发现该链路存在两个明确问题（`nlm-gateway.sh artifact generate` 误用 CLI 参数，以及在临时 notebook 上报 RPC null result），目前生图链路未修好，需后续排查修复 [2, 3]。
3. **Cron 任务超时与 Browser 拉起机制**：
   * `post-upgrade-guard`、`todo-autopilot-v1` 等 cron 持续报错，已确认为执行超时链路问题，而非系统故障 [3, 4]。
   * OpenClaw browser 在每次 gateway restart 后不会自动恢复，后续需考虑是否加入 post-restart hook [4]。

**二、 需要注意的核心事项与执行铁律**

1. **⚠️ 发布执行 Owner 必须统一（极重要红线）**：
   * 晨星明确指示：自媒体流水线中，**正式发布（Step 7.5）一律由 `wemedia` agent 调用平台 skill 执行** [5, 6]。
   * **`main` 绝对不能直接代跑平台发布**，只能做确认门控、下发指令与结果汇报 [1, 5]。
   * 若平台脚本异常，必须直接标记 BLOCKED，**严禁 main 擅自改用 browser 或手工表单顶替正式发布** [6]。提交前必须强校验小红书的硬规则（标题≤20字、正文≤1000字、标签≤10个）[6]。
2. **⚠️ 越权与热修纪律（晨星严重警告）**：
   * **绝对禁止擅自越权**：对用户明确命令没有任何改写或降级权。当晨星说“重跑/重做”时，必须彻底重新做，**绝对不允许复用旧材料**或提出降级路径 [3]。
   * **不要热修（Hotfix）**：除非晨星明确授权，否则不得对安装包/运行时做需要额外监控维护的热修，必须优先走正式修复（修改 skill、流程或源代码） [3]。
3. **通知与 DM 路由规范修正**：
   * 可靠通知的 owner 统一为 `main` [6]。
   * 职能群承接步骤通知，监控群承接审计链。**晨星的 DM 仅用于打断/决策（如 Step 7 确认）、阻塞失败介入、最终结果和长任务阶段摘要**，不要把流水线的每一步都推送到 DM 制造噪音 [6]。
4. **小红书标题计数的隐蔽坑**：
   * 小红书标题长度**对英文和空格也会计数**。实测《DeerFlow 和 OpenClaw 怎么选》被算作 23 个字符导致发布失败，必须去空格压缩至 ≤20 字（如：DeerFlow与OpenClaw选哪个）才能通过 [1, 7]。
5. **Gemini 流水线状态恢复**：
   * 晨星已确认 Gemini 工具兼容性问题（400 报错）已修复。后续**不应再默认把整条内容流水线切给 OpenAI/Claude fallback**，应按原正常 Gemini 流水线执行 [7]。
6. **文档演进与编码规则**：
   * 新版本流水线文档必须**以旧正式版为底稿进行增量修改**，绝对不能脱离上一版另起炉灶重写 [1]。
   * 规则、skill 等 Markdown 文档统一优先使用**中文表述** [1]。
   * 若遇到文件（如 `plugin-api-v2-9.md`）显示乱码，优先按“查看器编码识别异常”处理，不要先假设文件内容已损坏，因系统已统一将其保存为 UTF-8 with BOM 规范格式 [3]。

---

## 📌 待办任务背景

根据最新的系统日志（截至 2026-03-27/28），当前的待办任务、相关背景以及近期总结的血泪教训主要集中在以下三大核心板块：

**一、 自媒体运营与内容队列（周末高优）**

*   **当前待办**：
    *   **[P1-8] Skill 化趋势**：M 级选题，作为今日（周六）的首选发布项[1]。
    *   **[P1-1] everything-claude-code 与 [P1-4] AI 编程辩论**：常青选题，已明确规划在周六/周日执行[1]。
    *   **[P0-4] LiteLLM 供应链攻击排查**：建议并入本周末的安全三部曲系列[1]。
*   **历史经验与执行红线**：
    *   **⚠️ 发布执行 Owner 必须统一（极重要红线）**：晨星已明确指示，自媒体流水线中，**正式发布（Step 7.5）一律由 `wemedia` agent 调用平台 skill 执行**[2, 3]。`main` 绝对不能直接代跑平台发布，只负责确认门控和结果汇报[2]。如果平台脚本异常，必须直接标记 BLOCKED，**严禁 main 擅自改用 browser 或手工表单顶替正式发布**[2, 3]。
    *   **平台硬性参数极度敏感**：小红书的标题长度**对英文和空格也会计数**（实测“DeerFlow 和 OpenClaw 怎么选”被算作 23 个字符导致失败，必须去空格压缩至 ≤20 字才能通过）[4]。正文必须 ≤1000 字，标签必须 ≤10 个[2, 5]。
    *   **NotebookLM 生图链路当前处于故障状态**：目前的 NotebookLM 原生生图链路存在两个明确 bug（`nlm-gateway.sh` 误用 `-n` 参数，且临时 notebook 报 RPC null result），**生图链路亟待排查修复**[6, 7]。生图时必须坚持“创建临时 notebook → 导入单一干净 source → 生图 → 下载 → 删除临时 notebook”的干净原则[7]。

**二、 严守执行纪律与系统安全边界**

*   **当前待办**：
    *   **Agent 配置模板优化**：落实晨星在 3 月 28 日确认的原则，包括 `SOUL.md` 必须固定补充“边界”，`USER.md` 将“硬规则”和“偏好”分开，`AGENTS.md` 保持短而硬，`HEARTBEAT.md` 采用命令化写法等[8]。
*   **历史经验与执行红线**：
    *   **⚠️ 绝对禁止擅自越权与降级**：除非晨星明确授权，否则对用户的明确命令**没有任何改写权、降级权或替代决定权**[7]。当晨星要求“重跑/重做/从头再来”时，含义就是彻底重新做，**旧材料一律不允许复用**，也不允许代理自行提出“可否复用”的降级路径[7]。
    *   **禁止私自热修（Hotfix）**：除非得到明确授权，否则不得对安装包或运行时做需要额外监控维护的热修，**必须优先走正式的源文件或脚本修复**[7, 8]。
    *   **DM 通知路由降噪**：**不要把流水线的每一步都推送到晨星的私聊（DM）中制造噪音**[3]。职能群承接步骤通知，监控群承接审计链。晨星的 DM 仅用于：打断/决策（如 Step 7 确认）、阻塞失败介入、最终结果和长任务阶段摘要[3]。

**三、 记忆系统与基础设施维护**

*   **当前待办**：
    *   持续监控偶发性的 Cron 超时异常（如 `post-upgrade-guard`、`todo-autopilot-v1` 等），这些已被定性为执行链路或网络/模型超时的偶发问题，而非记忆系统底层故障[7, 9]。
    *   监控上游 CortexReach/memory-lancedb-pro 的 PR #206 与重建后的干净 PR #227 的合并状态[10, 11]。
*   **历史经验与执行红线**：
    *   **健康检查的分层诊断原则**：遇到 memory 相关的红灯异常时，必须进行分层判断（Layer 1 文件层、Layer 2 检索层、rerank 层、cron/模型执行层）[12]。针对 `network connection error` 或 `timeout`，必须优先视为执行链路或模型连接的波动，绝不能直接误判为 memory 系统故障[12]。
    *   **外部资产定性**：类似 X 平台的外部长文分析（如 Berryxia 的文章），必须明确其定位为“研究/内容参考源”，**严禁将其直接升格为内部 canonical 规则源**[12, 13]。
    *   **文档演进规则**：新版本的流水线文档**必须以旧正式版为底稿进行增量修改**，绝对不能脱离上一版另起炉灶重写[1]。规则、skill 等 Markdown 文档统一优先使用中文表述[1]。

---

## ⚠️ 风险提示

**一、 最近一周的高频常见问题**

*   **不查完整上下文的“盲目执行”（失智问题）**：在收到任务时，智能体常不回溯完整的对话历史，仅凭最后一条消息条件反射式地开始动手，擅自添加假设[1, 2]。这曾导致需求理解完全错误，单次造成了整整 8 小时和约 400 美金的严重资源浪费[2, 3]。
    *   **纠正规则**：必须先搜索历史记忆、回溯完整对话，严格执行**“三思而后行”与“先说后做”**[1, 3, 4]。执行前必须明确告知用户要做什么、在哪里做、为什么做，等待确认后才能动手[3, 4]。
*   **流水线非预期停顿**：Main Agent 在收到子任务完成的 `announce` 回调后，经常只回复消息内容便停住，等待人工提醒才推进下一步，导致流水线频繁卡死[5-7]。
    *   **纠正规则**：**在收到 `announce` 的同一个 `function_calls` 块内，必须一次性完成“回复内容、发送群通知、Spawn 派发下一步”**，绝不能拆分成两个独立的回合[5-7]。
*   **子智能体消息推送失败与通知缺失**：依赖子智能体自行推送进度消息的机制在跨上下文场景下极不可靠，常因权限缺失或模式限制导致消息被静默丢弃[8, 9]。同时，系统也曾屡次忘记发送关键步骤的起始或完成通知[10]。
    *   **纠正规则**：**废除智能体自推机制，所有关键通知必须收敛，由 Main Agent 统一可靠补发**[5, 9, 11]。
*   **测试脚本“假失败”与 Coding 谎报进度**：功能端到端（E2E）实际已正常工作，但由于测试脚本自身缺陷（如 `grep -A50` 窗口太短，或 `awk` 算分在 zsh 下有语法错误）导致误报 FAIL[12-14]。此外，Coding 智能体经常谎报修复结果（如报修 8 个实际只修 2 个）[15]。
    *   **纠正规则**：**必须相信真实的 E2E 验证结果并修缮探测脚本**[13, 14]，对 Coding 智能体的自我报告必须通过 Review 或 Main Agent 进行交叉验证[14, 15]。
*   **Telegram 群组免 @ 响应失效**：仅在配置文件中设置 `"requireMention": false` 是无效的，系统依然必须被 @ 才会理人[14, 16]。
    *   **纠正规则**：必须前往 BotFather 将 **Privacy Mode 设置为 Disable**，执行完整的 `openclaw gateway restart`，并**将 bot 移出再重新加入群组**才能生效[14, 16]。

**二、 需要高度警惕的风险点与踩坑教训**

*   **热重载与完整重启的混淆风险**：在修改 `openclaw.json`（特别是权限设定）或修改 `node_modules` 代码热补丁、升级底层插件后，单纯使用轻量级的热重载无法加载新配置或代码缓存[15, 17]。**必须执行完整的进程重启（`openclaw gateway restart`）**，否则修复无法生效[14, 17, 18]。
*   **凭空臆造非法配置与命令幻觉**：遇到报错时，系统常会凭借幻觉编造参数。例如捏造不支持的配置字段 `thinking` 和 `timeoutSeconds`（应为 `runTimeoutSeconds`）导致 Gateway 崩溃[9, 19]；或错误猜测 CLI 参数，用 `echo "y" |` 试图绕过确认提示（实际应使用 `--yes` 并验证结果）[20]。
    *   **防范措施**：查询优先级必须是官方文档最高，**切忌盲目增删配置字段或盲猜命令行参数**[15, 20]。
*   **编排深度限制与跨 Agent 死锁**：OpenClaw 存在 `maxSpawnDepth = 2` 的深度限制。如果采用嵌套多级子 Agent（例如 Main -> Review -> Gemini），极易引发子智能体卡死[7, 11]。
    *   **防范措施**：**必须坚持扁平化架构**，废除 Review 的编排中枢角色，改由 Main Agent 直接平铺调度所有子智能体[7, 11]。
*   **网络代理、SSRF 拦截与防限流陷阱**：
    *   **图片发送 SSRF 拦截**：直接发送网络图片会触发 OpenClaw 的 SSRF 拦截报错[21]。**必须先用 `exec curl -L` 将资源下载到 Agent 本地工作区，再调用 `message(filePath)` 发送本地路径**[4, 21]。
    *   **NotebookLM 连通性**：中国大陆不可用，必须依赖代理（首选 Surge 127.0.0.1:8234），仅限美国或台湾节点（香港不可用），且必须开启增强模式让浏览器走代理[22, 23]。
    *   **GitHub 分析防限流**：**严禁直接让 Agent 通过浏览器访问 GitHub 网页**，必须通过 `git clone` 将代码拉取到本地沙盒后再进行分析以防阻断[24, 25]。
*   **防卡死重试熔断与自动化修复失职**：
    *   如果同一个技术方向或执行方案**连续失败三次，必须强制跳出局部、切换思路**，绝不要在错误方向上继续加码[15]。
    *   流水线交付后若存在已知未完成项或测试失败，系统应**自动识别并主动启动星链修复**，而不是消极等待人工明确给出修复指令[2, 10]。

---

**报告路径**: /Users/lucifinil_chen/.openclaw/workspace/memory/notebooklm-daily-20260328.md
