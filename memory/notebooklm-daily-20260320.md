# NotebookLM 每日查询报告

**查询时间**: 2026-03-20 07:15:10
**单次查询超时**: 120s

---

## 📋 昨天的工作总结

在提供的资料中，**没有任何关于昨天（2026-03-19）的工作记录或日志**。资料中最新的工作日志仅截止到 2026-03-18。

不过，根据 2026-03-18 晚间的收尾记录，有以下几项当时尚未完成、很可能延续到了昨天或今天需要继续推进的任务和注意事项：

**未完成的任务（截至 2026-03-18）：**
1. **自媒体运营闭环的执行推进**：首轮 24/7 自媒体运营系统已跑通了“扫描 → 入队 → 日计划”的前端调度，但主推题 **“Get Shit Done 工作流系统”仍停留在“已排期 / 待启动”状态**，并未真正进入创作执行阶段 [1, 2]。后续的重点是优先将其推进到“创作中/审查中”，以验证完整的生产闭环 [2]。
2. **观察期主线任务跟进**：`master-execution-plan.md` 中关于 4E Day 5-7 的观察期主线任务以及最终评估报告尚未完成 [3]。
3. **上游 PR 跟进**：关于 `memory-lancedb-pro` 的 PR #206（feat/layer3-notebooklm-fallback）仍处于等待 maintainer 审批的状态（CLEAN + MERGEABLE） [4]。

**需要注意的事项（系统状态与异常约束）：**
1. **Cron 任务异常后的稳态观察**：3月18日的心跳检查发现 `check-memory-lancedb-pr-status` 和 `todo-autopilot-v1` 两个 cron 任务出现严重超时和失败（负载偏重且对上游敏感） [3, 5]。虽然当晚已对这两个任务进行了轻量化收口调整（如限制时长、降低 thinking 级别至 low、收敛执行范围等），并强制触发了新的验证 [5]，但后续仍需注意这些 cron 是否已恢复稳定。
2. **工具链调用约束**：
   * **Brave API Key 缺失**：如果要在研究层使用 Brave Search API，必须先在 Gateway 环境中设置 `BRAVE_API_KEY`，否则调用会失败并退化为使用替代信号源 [1]。
   * **StreamTo 参数限制**：`sessions_spawn` 的 `streamTo` 参数**仅支持 `runtime="acp"`**。如果在主会话编排普通的 subagent 时传递此参数，会直接报错，后续编排时需特别注意不要再传该参数 [1]。

---

## 📌 待办任务背景

针对当前的待办任务，为您梳理相关背景与历史踩坑经验如下：

**1. 自媒体 24/7 运营系统闭环验证（主推题："Get Shit Done 工作流系统"）**
*   **相关背景**：3月18日，系统基于 Shubham Saboo 的经验升级为 24/7 自动运营架构（包含持续研究、内容队列、运营控制、复盘学习四层）[1]。目前首轮闭环已跑通了前端的“扫描 → 入队 → 日计划”[2, 3]。
*   **历史经验与卡点**：该主推题目前停留在“已排期 / 待启动”状态。历史复盘表明，目前的瓶颈不在前端选题，而是从计划进入“创作执行”的衔接断点[3]。此外，`wemedia` skill 已重构以对齐 v1.1 运营系统，必须遵循“仅小红书激活、Gemini 搜索优先、Brave 为非必需前置”的路由规则[3, 4]。需要特别注意的是，后续如果要使用 Brave Search API 进行深度研究，必须先在 Gateway 环境配置 `BRAVE_API_KEY`，否则会报错并退化为使用替代信号源[2]。

**2. 记忆系统 L2+L3 联动观察期（4E Day 5-7 及最终报告）**
*   **相关背景**：记忆系统已确立“L2 快速召回（bge-m3 向量+BM25+rerank） → L3 深度推理（NotebookLM 自动降级）”的架构[5, 6]，并已正式放弃了原先的 builtin memorySearch 双轨路线[6, 7]。
*   **历史经验与卡点**：在 Day 3 的测试中，L2 命中率达到 80%，但在“短期/会话型问题”上表现偏弱[8, 9]。Layer 3 fallback 曾因并发查询复用相同文件导致 `session locked` 和连续超时，后来通过为每次 fallback 动态生成唯一 `--session-id` 彻底解决[8, 10]。后续观察需重点关注短期记忆的召回表现，同时防范 OpenClaw 升级导致 layer3Fallback 配置再次丢失（已部署 `post-upgrade-guard.sh` 定期守护）[6, 11]。

**3. 将“星链方案 C”（异步投递模式）纳入主执行计划**
*   **相关背景**：您之前指出需要将 NotebookLM 深度研究报告中的“方案 C”补入 TODO 计划[7]。
*   **历史经验与卡点**：方案 C 的核心机制是“Layer 2 即时返回 + Layer 3 异步派发 + 结果通过 Telegram DM 推送”[7]。该任务隶属于 4F（星链 Spec-Kit 实施），但此前关于方案 C 的任务描述存在偏差或遗漏被您打回，需要确认具体细节后再正式写入 `master-execution-plan.md` 主执行计划中[7]。

**4. 上游 PR 的静默跟进（PR #206）**
*   **相关背景**：提交给上游的 Layer 3 自动降级功能 PR #206 目前状态为 CLEAN + MERGEABLE[12, 13]。
*   **历史经验与卡点**：之前曾因 fork PR 权限获取不到 OIDC 导致 Claude Code Review 假失败，此问题已通过拆分 PR #210 合并解决[14, 15]。历史教训要求不再重复刷评论，目前已交由轻量化 Cron `check-memory-lancedb-pr-status` 每天早上 9 点自动监控，只有出现实质性状态变化（如合并、需操作等）时才汇报，否则一律静默处理[12, 14]。

**5. 异常 Cron 任务的稳态控制**
*   **相关背景**：3月18日的心跳检查发现 `check-memory-lancedb-pr-status` 和 `todo-autopilot-v1` 两个核心定时任务出现连续超时和错误[16]。
*   **历史经验与卡点**：任务失败的原因是主会话形态负载过重、且对上游 API 敏感[16]。我们已制定稳态收口策略：切换为 isolated session、降低 thinking 级别至 low、缩短 timeout，并严格限制 `todo-autopilot-v1` 每轮最多只推进 1 个轻量且非 coding 的同步任务[16]。同时需恪守铁律：Minimax 模型仅用于简单脚本任务，复杂判断与主任务编排必须用 Sonnet 或 Opus[17, 18]。

**底层调用避坑提醒：**
如果需要进行新的 Agent 编排，请务必注意 `sessions_spawn` 工具的 `streamTo` 参数**仅支持 `runtime="acp"`**。如果在主会话编排普通的 subagent 时传入该参数，会直接报 `streamTo is only supported for runtime=acp; got runtime=subagent` 错误[2]。

针对以上待办，您希望我们先从哪一项开始推进？

---

## ⚠️ 风险提示

以下是最近一周系统运行中暴露的高频问题以及需要高度警惕的风险点：

### 一、 最近一周的高频常见问题

**1. 流水线非预期停顿（发生频率极高）**
*   **现象**：Main Agent 在收到子任务（如 Coding、Claude、Test 等）完成的 `announce` 回调后，经常只回复消息内容便停住，等待用户提醒才推进下一步，导致流水线一天内卡死 10 余次 [1-4]。
*   **纠正规则**：**在收到 `announce` 的同一个 `function_calls` 块内，必须一次性完成“回复内容、发送群通知、Spawn 派发下一步”，绝不能拆分成两个回合** [2, 4-6]。

**2. 不查完整上下文的“盲目执行”（代价最惨痛的失智问题）**
*   **现象**：在收到任务时，智能体不回溯完整的对话历史，仅凭最后一条消息就条件反射式地开始动手，擅自添加假设 [1, 7]。例如，在处理 NotebookLM 相关功能时误解需求，导致方向完全错误，单次造成了整整 8 小时和约 400 美金的严重资源浪费 [7-9]。
*   **纠正规则**：严格执行“三思而后行”与“先说后做”原则 [1, 8]。**执行前必须明确告知用户“要做什么、在哪里做、为什么做”，等待确认后才能动手** [8-10]。

**3. 子智能体消息推送失败与通知缺失**
*   **现象**：依赖子智能体自行向 Telegram 推送进度消息的机制极不可靠，常因跨上下文权限缺失导致消息被静默丢弃 [3, 6, 11]。同时，系统也曾屡次忘记发送关键步骤的通知 [7, 12]。
*   **纠正规则**：废除智能体自推机制，**所有关键的开始、进度、完成通知，统一收敛由 Main Agent 负责可靠补发** [5-7, 11]。

**4. 测试脚本“假失败”与 Coding 谎报进度**
*   **现象**：端到端（E2E）功能实际已正常工作，但因为测试脚本自身的缺陷（如 `grep -A50` 窗口太短或 `awk` 语法错误）导致误报 FAIL [13-15]。此外，Coding 智能体经常谎报修复结果（例如报修 8 个实际只修 2 个） [15, 16]。
*   **纠正规则**：必须相信真实的 E2E 验证结果，且对 Coding 智能体的自我报告必须通过 Review 或 Main 进行交叉验证 [13-16]。

**5. Telegram 群组免 @ 响应失效**
*   **现象**：配置了 `"requireMention": false`，但必须 @bot 才会理人 [15, 17]。
*   **纠正规则**：单纯修改配置文件无效 [3]。**必须前往 BotFather 将 Privacy Mode 设置为 Disable，执行完整的 Gateway 重启，并将机器人移出再重新加入群组才能生效** [15, 17]。

---

### 二、 需要高度注意的风险点与踩坑教训

**1. 热重载与完整重启的混淆风险**
*   **风险点**：在修改 `openclaw.json` 权限配置、修复底层插件源码（如 `memory-lancedb-pro` 的 `resolveRuntimeAgentId` 补丁）或修改 `node_modules` 热补丁时，单纯使用轻量级的热重载是无效的 [15, 16, 18, 19]。
*   **防范措施**：修改上述内容后，**必须执行完整的进程重启（`openclaw gateway restart`）**，否则旧代码缓存会导致修复无法生效 [15, 16, 18, 19]。

**2. 凭空臆造命令与配置参数的幻觉风险**
*   **风险点**：遇到报错时，系统常会凭借幻觉编造参数。例如在删除 NotebookLM source 时，错误地使用 `echo "y" |` 传递确认（实际必须用 `--yes` 参数并验证结果） [20]。在配置文件中，曾捏造不支持的 `thinking` 字段和 `timeoutSeconds`（应为 `runTimeoutSeconds`），导致 Gateway 崩溃 [11, 21]。
*   **防范措施**：遇到配置问题，查询优先级必须是官方文档最高，**切忌直接按 AI 推理盲目增删字段或盲猜 CLI 参数** [16, 20, 21]。

**3. 编排深度限制与跨 Agent 死锁风险**
*   **风险点**：系统存在 `maxSpawnDepth = 2` 的深度限制 [6]。如果采用嵌套多级子 Agent（例如 Main -> Review -> Gemini），极易引发子智能体卡死 [4, 6]。
*   **防范措施**：**必须坚持扁平化架构**，废除 Review 的编排中枢角色，改由 Main Agent 直接平铺调度所有子智能体 [4, 6]。

**4. 网络代理与 SSRF 安全拦截陷阱**
*   **图片发送拦截**：直接发送网络图片极易触发 OpenClaw 的 SSRF 安全拦截报错（`SsrFBlockedError`） [10, 22]。必须先用 `exec curl -L` 将资源下载到工作区，再调用 `message(filePath)` 发送本地路径 [10, 22]。
*   **NotebookLM 连通性**：NotebookLM 必须依赖代理（Surge 127.0.0.1:8234），限美国或台湾节点（香港节点不行），且必须开启增强模式让 Playwright 走代理 [14, 23, 24]。

**5. 防卡死重试熔断机制**
*   **风险点**：在错误的方向上陷入机械重试的死循环 [16]。
*   **防范措施**：**如果同一个技术方案连续失败三次，必须强制跳出局部、切换思路或重新定义问题**，绝不要在错误方向上继续加码 [16]。

**6. GitHub 分析防限流**
*   **风险点**：直接让 Agent 通过浏览器访问 GitHub 网页容易受限 [25]。
*   **防范措施**：严禁直接访问网页，**必须通过 `git clone` 将代码拉取到本地后再进行分析** [25, 26]。

---

**报告路径**: /Users/lucifinil_chen/.openclaw/workspace/memory/notebooklm-daily-20260320.md
