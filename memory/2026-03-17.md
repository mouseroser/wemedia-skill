# 2026-03-17

## 07:30 记忆观察期 Day 3 完成

- 按 `master-execution-plan.md` 的 **4E.3 Day 3（03-17）** 跑完 15 条 memory benchmark，并生成报告：`reports/memory-retrieval-scorecard-20260317.md`。
- 今日结果：**L2 可用命中 12/15 = 80%**，**平均质量 4.1/5**。长期规则 / 稳定事实类问题基本稳定；短期 / 会话型问题（比如“刚才新建的 TODO 文件叫什么”“当前策略是什么”）仍偏弱。
- 暴露出的精确值盲点：`记忆压缩检查的阈值是多少` 未能稳定召回 `40k tokens`。
- 今日 benchmark 中 **L3 被动触发 5/15**，但 **0 次形成有效补充**；其中 **3 次显式失败**，错误集中在 `notebooklm` session file locked + gateway timeout。这说明当前 Layer 3 fallback 在检索链路里仍有可用性问题，需要单独排查。
- 已同步更新：
  - `reports/memory-retrieval-observation-20260314.md`
  - `~/.openclaw/todo/master-execution-plan.md`
  - `~/.openclaw/todo/memory-retrieval-optimization-2026-03-14.md`

## 07:55 修复 Layer 3 fallback 连续 3 次 session lock

- 根因确认：`memory-lancedb-pro/src/tools.ts` 里的 `runNotebookLMFallbackQuery()` 调 `openclaw agent --agent notebooklm ...` 时没有显式 `--session-id`，导致多次 Layer 3 fallback 会复用同一个 NotebookLM session 文件，撞上 `~/.openclaw/agents/notebooklm/sessions/*.jsonl.lock` 后连续 timeout。
- 已修复：在 runtime plugin 里为每次 Layer 3 fallback 生成唯一 session id（`memory-layer3-fallback-*`），做成最小变更，不改现有 config 语义。
- 回归测试：`test/layer3-fallback.test.mjs` 已新增覆盖，验证每次 fallback 命令都有 `--session-id` 且不同调用 id 不同。
- 验证：`node --test test/layer3-fallback.test.mjs`、`npm run typecheck`、`npm run build` 全部通过；修复 commit：`567a59c561494e91e92f60da890839fe027d5be9`。
- 运行态已处理：按旧教训先清 `/tmp/jiti` 缓存，再用 gateway restart 让 live runtime 吃到新代码；随后做了一次真实 `memory_recall` 冒烟，未再出现 `notebooklm session lock`。

## 结论

1. **Layer 2 已足够承担主链路**：稳定规则和长期事实召回整体可用。
2. **短期记忆仍弱**：会话级“刚才决定了什么”这类问题暂时不能完全依赖 Layer 2。
3. **Layer 3 的“连续 3 次 session lock”根因已修掉**；后续如果还有问题，更可能是回答质量/内容为空，而不是 session 文件争锁。 

## 08:36 Layer 3 根因二次校正：`--session-id` 不够，需绕开 gateway lane

- 对 runtime / CLI 路由继续深挖后，确认前一版“给 `openclaw agent --agent notebooklm` 补唯一 `--session-id` 就能隔离 session 锁”的判断**不成立**。原因是 `--session-id` 只影响 transcript id，不改变 gateway 的 agent lane / sessionKey；请求仍会落到固定的 `session:agent:notebooklm:main` 轨道上，因此仍可能撞同一把 `.jsonl.lock`。
- 现场证据：直接跑 `bash ~/.openclaw/skills/notebooklm/scripts/nlm-gateway.sh query --agent main --notebook memory-archive --query '当前 Layer 2 和 Layer 3 的策略对比，完整一点'` 能正常返回结构化 JSON，说明 NotebookLM 本体与 notebook 数据没坏，问题集中在 `openclaw agent --agent notebooklm` 这层 gateway/lane 路由。
- 新的最小可靠修复：把 `memory-lancedb-pro/src/tools.ts` 的 Layer 3 fallback 调用从 `openclaw agent --agent notebooklm ...` 改为**直接调用** `~/.openclaw/skills/notebooklm/scripts/nlm-gateway.sh query --agent notebooklm --notebook memory-archive --query ...`，保留现有 `layer3Fallback.agent/notebook/notebookId/timeout/triggers` 配置契约不变，但彻底绕开 gateway lane 锁争用。
- 已同步更新回归测试：`test/layer3-fallback.test.mjs` 不再断言 `--session-id`，改为断言 fallback 命令直接落到 `nlm-gateway.sh query`。
- 当前 runtime worktree 新提交：`36d6c4a`，message：`fix: use nlm-gateway.sh for layer3 fallback to avoid gateway lane lock`。
- 当下状态：`rm -rf /tmp/jiti && openclaw gateway restart` 已执行，gateway 报告 `restart scheduled`；**是否完全吃到新代码仍待重启后再做一次真实 `memory_recall` 复测确认**。

## 10:26 Layer 3 复测收口：链路已通，剩余是 45s 偶发超时而非 session lock

- 重启后已确认 live runtime 运行在新代码：gateway pid `70635`，runtime worktree HEAD `687420d`（`fix: honor configured timeout for nlm gateway layer3 fallback`）。
- 追加 benchmark / 冒烟结果：
  - `当前 Layer 2 和 Layer 3 的策略对比，完整一点` → Layer 3 成功返回 ✅
  - `本周记忆系统有哪些重要变化？请详细说明` → Layer 3 成功返回 ✅
  - `为什么之前会出现 memory-lancedb-pro beta.8 回归？` → 第一次 `memory_recall` 偶发报 `nlm-gateway terminated by SIGTERM (timeout 45000ms)`，但手工同题直跑 `nlm-gateway.sh query` 实测仅 `22.971s`，随后第二次 `memory_recall` 复测成功 ✅
  - `昨天的工作总结详细历史` → Layer 3 成功返回 ✅
- 结论更新：今天这条主故障已经从“连续 3 次 session lock”收敛为“**session lock 已清零，剩余是 Layer 3 深查偶发抖动 / 45s timeout 偏紧**”。
- 同时确认两个此前报超时的 cron 已恢复正常：
  - `02f73862-87f7-4a82-a02b-a9717cbbea6b`（openclaw-runtime-audit）最近两次手工触发都成功，耗时 `63s` / `104s`
  - `6cf8cc6e-3b23-4ec7-a1d5-5510f7287e8c`（NotebookLM 周同步）成功，耗时 `17s`，且已 delivered
- 当前剩余动作：如果后续还想继续压低 Layer 3 偶发超时，优先方案不是再改路由，而是**把 `plugins.entries.memory-lancedb-pro.config.layer3Fallback.timeout` 从当前 `45` 秒调高**；这属于配置决策，不在本轮默认自动改。

## 10:43 Layer 3 timeout 配置已提升到 75，并完成边界验证

- 经晨星确认，已通过 `gateway config.patch` 将 `plugins.entries.memory-lancedb-pro.config.layer3Fallback.timeout` 从 **45 秒调到 75 秒**，并自动重启 gateway。
- 重启后已再次确认 runtime 正常运行；随后补做了“接近旧上限”的边界验证：
  - `nlm-gateway.sh query --no-cache` 针对新重 query `当前 Layer 2 和 Layer 3 的策略对比，完整一点，包含定位、技术栈、触发机制、守护机制、修复历史和当前风险评估` 实测 **46.607s**。
  - 同题 `memory_recall` 在新配置下成功返回 **Layer 3 (NotebookLM)** 结果，说明新的 **75 秒上限已真实接住旧 45 秒会越界的查询**。
- 今日关于 Layer 3 fallback 的最终结论更新为：
  1. **gateway lane / notebook session lock 根因已修掉**；
  2. **旧的 45s timeout 偏紧问题已通过配置调优修掉**；
  3. 当前链路已从“反复失败”进入“可用 + 观察期”状态。
- 2026-03-17T02:50:31.529Z [decision:agent:main] agent=main source=memory_store 【Layer 3 fallback timeout 调整为 75 秒】2026-03-17：在确认 gateway lane/session lock 根因已修复后，继续用 nlm-gateway --no-cache 做边界验证，发现重 query 实测 46.607s，超过旧的 45s 上限。经配置变更将 plugins.entries.memory-lancedb-pro.config.layer3Fallback.timeout 从 45 调到 75，并完成同题 memory_recall 成功验证。结论：Layer 3 fallback 当前已从反复失败收口为可用+观察期。
- 2026-03-17T02:50:55.440Z [entity:agent:main] agent=main source=auto-capture System: [2026-03-17 10:42:19 GMT+8] Exec completed (calm-cor, code 0) :: 27.697s stdout_chars=14186 stderr_chars=0 answer_preview= 之前出现 `memory-lancedb-pro@1.1.0-beta.8` 回归的根本原因是：**插件的升级操作直接覆盖了之前打在本地源码 (`src/tools.ts`) 上的修复补丁** [1, 2]。 具体的技术细节和衍生出的踩坑 -…  Conversation info (untrusted metadata): ```json {   "message_id": "23545",   "sender_id": "1099011886",   "sender": "快樂小豬 🐷",   "timestamp": "Tue 2026-03-17 10:43 GMT+8" } ```  Sender (untrusted metadata): ```json {   "label": "快樂小豬 🐷 (1099011
- 2026-03-17T03:07:17.904Z [entity:agent:main] agent=main source=auto-capture System: [2026-03-17 10:42:19 GMT+8] Exec completed (calm-cor, code 0) :: 27.697s stdout_chars=14186 stderr_chars=0 answer_preview= 之前出现 `memory-lancedb-pro@1.1.0-beta.8` 回归的根本原因是：**插件的升级操作直接覆盖了之前打在本地源码 (`src/tools.ts`) 上的修复补丁** [1, 2]。 具体的技术细节和衍生出的踩坑 -…  Conversation info (untrusted metadata): ```json {   "message_id": "23545",   "sender_id": "1099011886",   "sender": "快樂小豬 🐷",   "timestamp": "Tue 2026-03-17 10:43 GMT+8" } ```  Sender (untrusted metadata): ```json {   "label": "快樂小豬 🐷 (1099011
- 2026-03-17T04:03:16.436Z [entity:agent:main] agent=main source=auto-capture 对比一下我的方向是否更优秀，还是各有千秋，给出今后的优化方向
- 2026-03-17T04:30:02.854Z [entity:agent:main] agent=main source=auto-capture 对比一下我的方向是否更优秀，还是各有千秋，给出今后的优化方向

## 11:17 PR #227 对齐上游并解冲突

- 在 `pr-187-refresh` worktree 中，将 `fix/memory-list-main-default-rebased` 重新对齐到最新 `upstream/master`，冲突点在 `src/tools.ts`。
- 解冲突时保留了**中心修复**思路：`resolveRuntimeAgentId()` 统一 fallback；没有重新引入调用点零散的 `|| 'main'` 补丁；同时保留回归测试 `test/runtime-agent-id-defaults.test.mjs`。
- 最终推送 commit：`22f2c78267a76d2edb27a66852885efd35f05d41`，最小验证 `node --test test/runtime-agent-id-defaults.test.mjs` 为 **3/3 通过**。
- GitHub 随后状态：PR #227 从 `CONFLICTING` 变为 **OPEN + MERGEABLE + UNSTABLE**；`version-sync` 通过、`cli-smoke` 跑起、`reviewDecision` 仍是 `CHANGES_REQUESTED`。本地 diff 基本只剩回归测试，说明上游已经包含等价的中心 fallback 逻辑。

## 12:01 PR #206 被关闭，Layer 3 上游化策略转向

- maintainer `chenjiyong` 关闭了 PR #206。给出的核心原因不是方向全错，而是**真实 OpenClaw agent E2E 在该分支回归**，同时 `memory_recall` 的响应契约从 `Found N memories:` 变成 `Layer 2 / Layer 3` 分层格式，破坏了现有集成面。
- 结论更新：**本地方案可继续演进，上游方案必须换打法**。不要再试图把“大一统 Layer 3 改造”一次性并进 master。
- 后续上游策略：优先拆成**小而兼容**的 PR（bugfix / runner hardening / opt-in 新能力）；保持现有 `memory_recall` 契约稳定；若继续推进 Layer 3，上游版本应把它收敛为**低置信度时才触发的稀疏 fallback**，而不是关键词驱动的默认增强，必要时改成新工具/新模式而非重写旧工具。
- 对本地路线的判断：晨星这套高配本地系统可以继续保留 richer output、NotebookLM 深推理和更积极的工程防护；对公共上游则应以兼容性和默认路径稳定性为先。

## 11:45 心跳检查补充：HEARTBEAT.md 的 cron 命令已过时

- 现场确认 OpenClaw `2026.3.13` CLI **没有** `openclaw cron trigger` 子命令，实际可用的是 `openclaw cron run <job-id>`。`HEARTBEAT.md` 里的触发命令因此已过时，后续执行时应以 `openclaw cron --help` 为准。
- 本次 heartbeat 发现 `openclaw-docs-update` 上次运行距今约 **55.7 小时**，超过 `HEARTBEAT.md` 规定的 **26 小时**阈值；已用 `openclaw cron run 772acaf3-b972-4f47-be34-c2ef655acf7d` 手动补跑，随后任务恢复为 `lastRunStatus=ok` 且已 delivered。
- 同轮检查里浏览器、rerank sidecar、`post-upgrade-guard.sh`、关键目录状态均正常；问题只在 heartbeat 文档中的 CLI 子命令写法落后于当前版本。


## 15:37 主执行计划完成结构化整理收口

- `~/.openclaw/todo/master-execution-plan.md` 已完成一次结构化整理：新增 **4H. Layer 3 上游兼容路线** 与 **M2.8** 里程碑；将“执行时间表”拆成 **历史快照 / 当前前瞻计划**；将“更新日志”拆成 **结构调整 / 状态变更**；并统一当前活动区块口径。
- 本轮重算后的主计划统计为：**总任务 86 / 已完成 35 / 未完成 51 / 完成率 41%**。来源统计表也已从“按来源文件”收口为更准确的“按来源”。
- 当前执行口径已明确：**4E 观察期当前进度 = Day 3/7 已完成，当前待办从 Day 4-7 开始**；历史周报/旧日志不再与当前待办混读。

## 15:38 两个要长期记住的边界

- **TODO 目录 repo 边界**：`~/.openclaw/todo/` 不在 `~/.openclaw/workspace` 这个 git repo 里，所以可以直接编辑，但**不能**和 workspace 改动一起 commit，也不能给出不存在的 workspace commit hash。以后遇到 repo 外路径，先确认仓库边界再谈 commit。
- **shell 管道 + heredoc 坑**：`cmd | python3 - <<'PY'` 这种写法会让 heredoc 占掉 stdin，导致 Python 拿不到 pipe 进来的内容；以后不要再用这种形式解析管道 JSON，改用 `python3 -c` / 临时文件 / 进程替换等明确读 stdin 的方式。
- 2026-03-17T08:17:56.904Z [decision:agent:main] agent=main source=memory_store 2026-03-17: TODO boundary defined as control plane, not system-level task ontology. Keep global todo directory at ~/.openclaw/todo/; master-execution-plan.md is the canonical global active queue/current execution state, while source todo files remain canonical for task semantics/details. Autopilot should only advance items admitted into master, not all local backlog.
- 2026-03-17T08:29:09.846Z [fact:agent:main] agent=main source=memory_store 2026-03-17: CortexReach/memory-lancedb-pro PR #227 received a maintainer reply from rwmjhb and was closed. Reason: the PR only adds a test file, does not modify runtime behavior, and the new test is not wired into `npm test`, so CI does not execute it. Maintainer suggested resubmitting only if the test is integrated into CI or the PR becomes a real behavior fix / runtime change.

## 16:35 TODO 控制面边界、PR #227 后续判断与 CLI 易错点

- **TODO 边界正式收口**：`~/.openclaw/todo/` 继续放在 workspace 外，定位为 **global active queue / control plane**，不是“系统级任务本体”也不是所有任务细节的唯一真相源。`master-execution-plan.md` 是**当前全局执行态 canonical**，源 todo 文件仍是**任务语义 / 细节 canonical**。`todo-autopilot` 只推进已纳入 master 的活跃项，不直接扫全部局部 backlog。
- **todo-daily-check 已改成 master-aware**：`~/.openclaw/skills/todo-manager/scripts/todo-daily-check.sh` 现在以 `master-execution-plan.md` 为 canonical，只统计优先级区块里的真实任务数，不再把 master 和源文件重复计数，也不再按章节标题数冒充任务数。skill repo commit：`0e0e234`（`fix: make todo daily check master-aware`）。
- **PR #227 关闭后的策略判断**：没有必要为了“满足 maintainer 给的形式条件”而立刻重提。只有当下一轮提案满足以下任一条件时才值得重提：
  1. 测试真正接入 `npm test` / CI 主链；
  2. PR 变成真实的运行时行为修复，并且 diff 足够小、可验证、不夹带其他 Layer 3 改动。
  否则应把精力放在 4H 后续更小、更兼容的上游提案，而不是机械地复活 #227。
- **OpenClaw CLI 易错点**：`openclaw cron runs` 需要显式 `--id <job-id>`，不能把 job id 当位置参数；否则会报 `required option '--id <id>' not specified`。以后查单个 job 的 run history 时先看 `openclaw cron --help`，不要想当然套用 `run <id>` 的位置参数形式。
- **Heartbeat 16:30 观察补充**：`post-upgrade-guard` 与 `check-memory-lancedb-pr-status` 最近一次调度都显示超时错误，但手动执行 `bash ~/.openclaw/scripts/post-upgrade-guard.sh` 返回 `RESULT=ok`；说明 cron 层仍可能有超时 / 执行预算问题，不能仅凭 `status=error` 就推断 Layer 3 配置已坏。

## 16:40 基于 PR #227 反馈重构 4H 上游策略

- 已将 `~/.openclaw/todo/master-execution-plan.md` 的 **4H. Layer 3 上游兼容路线** 重构为基于 maintainer 接受边界的两步走：**先 pure hardening 小 PR，再做 contract-safe / opt-in 的 strict fallback 提案**。
- 新增报告：`reports/layer3-upstream-priority-reset-20260317.md`，把后续动作明确拆成三档：
  - **先做**：PR-A（CLI runner hardening）+ `#227` 收口结论 + upstream-safe evidence skeleton
  - **后做**：新工具 vs behind flag 决策 + PR-B（strict fallback, contract-safe）
  - **暂不做**：原样重提 #227、机械复活 #206、只加测试但不接 `npm test` 的 PR、任何默认 contract 改写
- 关键判断：没有必要为了满足 maintainer 的形式条件而立刻重提 #227；只有当测试真正进入 `npm test` / CI 主链，或提案变成真实 runtime 行为修复时，才值得以新 PR 形式重用这条线。
- 同步校正 `shared-context/THESIS.md`：移除“PR #206 等待 maintainer 审批合并”的旧叙事，改为 4H 上游小 PR 序列化路线，避免后续按过时方向继续推进。
- 2026-03-17T08:44:51.981Z [decision:agent:main] agent=main source=memory_store 2026-03-17: 4H upstream strategy reset after PR #227 closure. Do not mechanically resubmit old PRs. New route is two-step: (1) infra-only PR-A for CLI runner hardening; (2) only after 4E evidence, decide new tool vs behind flag and then draft contract-safe, opt-in strict-fallback PR-B. Explicitly defer PR #206 revival, as-is #227 resubmission, test-only PRs not wired into npm test, and any default memory_recall contract changes.

## 16:52 撤回 PR-A scope 草案，4H 保留策略不启动执行

- 更正：在已经明确“当前不做上游执行性动作”的前提下，不应继续把 **PR-A** 实体化为 scope 草案。之前那一步已撤回。
- 已删除 `reports/layer3-pr-a-cli-runner-hardening-scope-20260317.md`，并把 `~/.openclaw/todo/master-execution-plan.md` 的 **4H.3** 改回“若未来恢复推进，PR-A 仍是首选入口（当前暂停）”。
- `shared-context/THESIS.md` 也已同步改成“4H 上游路线暂缓执行”，避免后续被误读成当前活跃工作。
- 保留的只有策略边界：**如果以后恢复推进，顺序仍是 PR-A → 接口边界决策 → PR-B；但本轮不启动。**
- 2026-03-17T08:55:55.352Z [decision:agent:main] agent=main source=memory_store 2026-03-17 correction: After deciding not to start upstream execution now, 4H should remain strategy-only. Do not actively draft PR-A/PR-B deliverables unless explicitly re-authorized. If upstream work resumes later, the preserved order is PR-A (infra-only hardening) -> interface boundary decision (new tool vs behind flag) -> contract-safe PR-B.
