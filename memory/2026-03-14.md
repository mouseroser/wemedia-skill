# 2026-03-14

## 今日记录

- 初始化当日日志文件，避免启动期读取 `workspace/memory/2026-03-14.md` 时报 `ENOENT`。
- 2026-03-14T02:23:33.804Z [decision:agent:main] agent=main source=memory_store 【升级后记忆系统回归修复】2026-03-14：升级到 memory-lancedb-pro@1.1.0-beta.8 后，`memory_stats` / `memory_list` 默认返回 0，但 `memory_recall` 仍可召回。根因是升级覆盖了 `src/tools.ts` 中 `resolveRuntimeAgentId(... ) || 'main'` 的本地补丁；仅做轻量 gateway reload 不够，必须执行完整 `openclaw gateway restart` 才会让插件源码修复真正生效。修复后 Layer 2 恢复：memory_stats=307, memory_list 正常。

## 10:24 升级后记忆系统验收与修复

- 升级到 `memory-lancedb-pro@1.1.0-beta.8` 后出现回归：`memory_stats` / `memory_list` 默认返回 0，但 `memory_recall` 仍可召回。
- 根因：升级覆盖了 `plugins/memory-lancedb-pro/src/tools.ts` 中本地补丁，`memory_stats` / `memory_list` 缺少 `resolveRuntimeAgentId(...) || 'main'`。
- 关键经验：只做 gateway 轻量重载不够，插件源码修复后必须执行完整 `openclaw gateway restart`，运行中的插件才会真正吃到修改。
- 验收结果：
  - Layer 1：`MEMORY.md`、`memory/`、`shared-context/`、`intel/` 正常
  - Layer 2：`memory_stats` = 307，`memory_list` 正常，`memory_recall` 正常，rerank sidecar 正常
  - Layer 3：`notebooklm list --json` 正常，`Memory Archive` notebook 可访问，`source list` 正常
  - 相关 memory cron 当前状态正常
- 2026-03-14T02:26:49.810Z [decision:agent:main] agent=main source=memory_store 【上游 issue / PR 重新打开】2026-03-14：由于升级到 memory-lancedb-pro@1.1.0-beta.8 后 `memory_list` / `memory_stats` 默认返回 0 的回归再次出现，已重新打开 CortexReach/memory-lancedb-pro Issue #186 和 PR #187，并补充了最新复现证据：默认路径失败、显式 `scope:"agent:main"` 正常、重新加回 `resolveRuntimeAgentId(...) || 'main'` 后恢复。

## 10:26 重新打开上游 issue / PR

- 已重新打开 CortexReach/memory-lancedb-pro 的 Issue #186 和 PR #187。
- 原因：升级到 `memory-lancedb-pro@1.1.0-beta.8` 后，同一回归再次出现：`memory_list` / `memory_stats` 默认返回 0，但 `memory_recall` 正常。
- 已在 issue / PR 中补充最新复现证据：
  - 默认路径失败
  - 显式 `scope:"agent:main"` 返回 307 条记忆
  - 重新加回 `resolveRuntimeAgentId(...) || 'main'` 后恢复
  - 需要完整 `openclaw gateway restart` 才会让插件源码修复真正生效
- 2026-03-14T02:28:46.015Z [preference:agent:main] agent=main source=memory_store 【语言规则补充】2026-03-14：凡是 GitHub / 上游 issue、PR、comment、commit message 等对外工程协作内容，一律使用英文；与晨星私聊仍使用中文。
- 2026-03-14T02:30:57.563Z [decision:agent:main] agent=main source=auto-capture 删除接口也卡了作用域，我换成带 `scope:"agent:main"` 的精确删除。
- 2026-03-14T02:30:57.596Z [preference:agent:main] agent=main source=auto-capture [[reply_to_current]] 好，已撤销。  我刚刚做了两件事： - 删掉了 `.learnings/LEARNINGS.md` 里那条“correction”记录 - 删掉了记忆库里那条“GitHub / 上游一律英文”的偏好记忆  这条不再算错误，也不再当成固定规则。
- 2026-03-14T02:38:49.892Z [preference:agent:main] agent=main source=memory_store 【GitHub 语言规则修正】2026-03-14：不要把 GitHub / 上游沟通语言一刀切定成英文。应按仓库与维护者语境决定语言。对于 `CortexReach/memory-lancedb-pro` 这类国人开发/中文语境仓库，issue、PR、comment、commit message 可以直接使用中文。与晨星私聊仍使用中文。

## 10:48 openclaw-docs 笔记已更新

- 因 OpenClaw 升级到 2026.3.12，手动触发 `openclaw-docs-update` 立即更新 openclaw-docs notebook。
- 更新结果：
  - 英文文档数量：358
  - 合并文件大小：3.0M
  - 删除旧 source：2 个
  - 新 source ID：`928ff98e-cca9-4f13-ac3f-5e8375f0e574`
- 已成功推送到珊瑚群和监控群。
- 2026-03-14T02:50:11.078Z [other:agent:main] agent=main source=auto-capture [[reply_to_current]] 📋 TODO 提醒：周六计划与观察期任务   ⏰ Deadline: 今天（周六）/ 2026-03-16（周日）/ 2026-03-27（观察期）   📊 当前状态：托管浏览器刚已自动拉起；rerank sidecar、关键目录、关键 cron 正常。未完成项包括：今天的 `MEMORY.md 优化`、`日志压缩检查`；周日的 `检查其他改进点`、`L8 安全审查`、`L7/L8 一致性检查`；观察期里的关键查询测试、召回准确率记录、冲突记忆召回验证也还未开始。
- 2026-03-14T03:08:41.709Z [decision:agent:main] agent=main source=memory_store 【OpenClaw 新版本联动规则】2026-03-14：只要晨星告诉我“OpenClaw 有新版本了”，我必须同步立即更新 openclaw-docs 笔记（NotebookLM 的 `openclaw-docs` notebook），不需要再等额外提醒。标准动作：确认版本变化 → 触发 `openclaw-docs-update` → 回报更新结果。

## 11:08 OpenClaw 新版本联动规则

- 与晨星约定：以后只要晨星告诉我“OpenClaw 有新版本了”，我就必须同步立即更新 NotebookLM 的 `openclaw-docs` 笔记，不再等待二次提醒。
- 标准动作：确认版本变化 → 触发 `openclaw-docs-update` → 回报更新结果。
