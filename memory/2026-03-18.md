## 08:19 记忆系统降噪与稳态修复

- 清理了 Layer 2 记忆库中的系统包络噪音：直接从 LanceDB 底表删除包含 `Conversation info (untrusted metadata)` / `Sender (untrusted metadata)` 的条目，并顺带清除了混入的 WhatsApp gateway connect/disconnect 状态噪音。清理后 memories 从 406 降到 392，相关包络噪音剩余 0 条。
- 清理了 Layer 2 的 exact duplicate：按 `text + category + scope` 做完全去重，保留每组首条、删除后续重复，共清掉 9 条，随后又保守清理了 1 条近似重复，优先保留带时间与“晨星原话”的证据型记忆。
- 为 memory-lancedb-pro 运行时插件增加了更强的噪音过滤规则：屏蔽 `Conversation info (untrusted metadata)`、`Sender (untrusted metadata)`、`Replied message (...)`、`System: [...]`、`message_id/sender_id/reply_to_id/has_reply_context/was_mentioned`、WhatsApp gateway connect/disconnect，以及 TODO 提醒、heartbeat ack、每日审计/状态报告、临时 cron/健康摘要等运营型噪音。
- 同步强化了 smart extraction prompt：明确禁止把系统包络、连接日志、状态摘要写入长期记忆；若“原话证据”和摘要版同义，优先保留原话证据，跳过无新增信息的摘要重述。
- 按规则清理 `/tmp/jiti/` 并重启了 gateway，确认新的记忆过滤规则已经生效。runtime 插件提交：`2a17b78 harden memory noise filtering rules`。
- 顺手修复了 memory-lancedb-pro 的旧 typecheck 问题：`src/llm-client.ts` 中 `response.json()` 返回 `unknown` 导致 `choices` 访问报错。采用最小安全修复（补明确响应类型、保留现有运行时 string guard），`npm run typecheck` 已恢复通过。runtime 插件提交：`6f85fad fix llm client response typing`。
- 踩坑记录：本机 Python 环境下 `lancedb.Table.to_lance()` 依赖缺失的 `lance/pylance`，会报 `ModuleNotFoundError: No module named 'lance'`；后续在这台机器上检查 LanceDB 内容时，应优先使用 `to_arrow()`。
- 2026-03-18T11:10:21.940Z [decision:agent:main] agent=main source=memory_store 【自媒体24/7运营系统 v1 方案】2026-03-18：晨星基于 Shubham Saboo 的 X 文章（How I built an Autonomous AI Agent team that runs 24/7）设计了完整运营系统升级方案。核心思路：把现有自媒体流水线从"接到任务才跑"升级成"全天候运营系统"。四层升级：A.持续研究层（早/午/晚三段扫描）B.内容队列层（Hot/Evergreen/Series三队列）C.运营控制层（排期+平台策略）D.复盘学习层（结构化反馈沉淀）。角色映射：main=总编排, gemini=快速研究+审查, notebooklm=深度研究, wemedia=主创作, nano-banana=视觉, openai=仲裁挑刺, claude=内容策略。MVP五点：1.每天3次研究扫描 2.自动维护HOT-QUEUE.md 3.main每天自动生成DAILY-PUBLISH-PLAN.md 4.最高优先级内容自动跑完整流水线 5.晚间自动日结复盘。晨星选择执行选项B（可执行落地清单）。

## 20:38 工具链配置与调用约束补记

- `web_search` 当前失败原因已确认不是临时网络波动，而是 **Gateway 环境缺少 `BRAVE_API_KEY`**。直接调用返回 `missing_brave_api_key`，提示可通过 `openclaw configure --section web` 或在 Gateway 环境中设置 `BRAVE_API_KEY`。结论：研究层若要使用 Brave Search API，必须先完成该配置；未配置时系统会退化为依赖 Hacker News 等替代信号源。
- `sessions_spawn` 的 `streamTo` 参数 **仅支持 `runtime="acp"`**，不支持 `runtime="subagent"`。本次直接报错：`streamTo is only supported for runtime=acp; got runtime=subagent`。后续 main 在主会话编排普通 subagent 时，不要再传 `streamTo`。
- 自媒体 24/7 运营系统首轮闭环已跑通到“扫描 → 入队 → 计划”三层：根据首轮信号扫描结果，main 已生成 `intel/media-ops/HOT-QUEUE.md` 与 `intel/media-ops/DAILY-PUBLISH-PLAN.md`，当前今日主推题为 **Get Shit Done 工作流系统**。
- 2026-03-18T13:19:28.145Z [decision:agent:main] agent=main source=memory_store 2026-03-18：关于 OpenClaw 文档查询路径，晨星明确纠正：默认不要直接读本地 docs 文件，因为太浪费 tokens；应优先使用 openclaw-doc 笔记/NotebookLM，它承载 OpenClaw 官方完整英文文档且更适合高效检索。仅在需要精确字段、原文片段或 notebook 未覆盖时，才回退到本地 docs 文件。

## 自媒体运营

- 今日主计划仅 1 条：**Get Shit Done 工作流系统**（M 级，小红书，Hot Queue 来源）。晚间复盘时状态仍停留在 **已排期 / 待启动**，未进入创作中。
- HOT-QUEUE 最终状态：
  - `Get Shit Done 工作流系统` → 已排期（未实质开工）
  - `2行代码启动沙盒 Agent` → 待评估
  - `Mistral AI Forge 实测` → 待验证
  - `Unsloth Studio Fine-tuning` → 储备
- 今日最重要结论：首轮 24/7 自媒体运营系统已跑通 **扫描 → 入队 → 日计划**，但 **计划未推进到生产闭环**；当前瓶颈不是选题，而是从计划进入创作执行的衔接。
- 被打回/未推进原因记录：今天没有出现“晨星打回文案”的反馈样本；真正的问题是 **执行断点发生在计划之后**，因此没有产生可复盘的内容质量反馈。
- 明日重点：优先把 `Get Shit Done 工作流系统` 从“已排期”推进到“创作中/审查中”，否则运营系统会停留在前端调度层，无法验证完整闭环。

## 21:35 wemedia skill 对齐运营系统架构

- 已更新 `~/.openclaw/skills/wemedia/SKILL.md` 的顶层契约，使其不再停留在“收到任务再生产内容”的旧流水线视角，而是对齐到 **自媒体运营系统 v1.1**。
- 关键修正：
  - 前言与 description 改为 24/7 运营系统描述；
  - 架构总览新增 Step 0 持续研究层、Step 1 内容队列层、Step 1.5 Publishability Gate、Step 8 日结/周复盘；
  - 当前范围明确为 **仅小红书激活**；抖音/知乎模板保留但默认不启动；
  - 搜索策略明确为 **Gemini 搜索优先**，`web_fetch` 抓正文，`browser` 复杂页面兜底，Brave 非当前必需前置；
  - `Agent Roles` / `Model Baseline` / `Main 编排流程` / 分级路由 已同步到运营系统版本。
- 结论：架构性更新后，wemedia skill 确实需要同步；否则会出现 agent 配置已经按运营系统运行，但 skill 仍在提示旧的按需创作流水线，造成执行偏差。
