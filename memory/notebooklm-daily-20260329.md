# NotebookLM 每日查询报告

**查询时间**: 2026-03-29 06:01:58
**单次查询超时**: 120s

---

## 📋 昨天的工作总结

关于昨天（2026-03-28）的工作（主要体现在 29 日凌晨的复盘与后续计划中），未完成的任务和需要注意的核心事项如下：

**一、 未完成的任务与后续落地待办**

*   **方案 B 落地（wemedia 强制拆分 spawn）**：目前流程存在 wemedia 端到端包揽的问题。下一步必须将关键步骤（Step 2A/2B/2C/4/5）强制拆分，分别交由对应的 agent（如织梦、小曼、小克、珊瑚）独立执行，并要求它们将进度推送到各自的职能群[1-3]。
*   **启动 `last30days-skill` 内容生产**：这已被明确规划为接下来的首条内容生产任务（属于 Skill 量化系列）[3]。
*   **A 篇（superpowers × OpenClaw 主题）配图问题决策**：该篇已于 28 日发布，但配图错误地使用了 Picasa 裁剪，而不是系统规定的 NotebookLM 原生方图。目前需要等待晨星决定是否需要撤帖重发[1, 3]。

**二、 暴露的问题与需要注意的核心事项（执行铁律）**

*   **⚠️ `announce` 机制不可靠与强制采用 `sessions_send`**：在 `mode=run` 的 isolated session 中，`announce` 无法保证可靠触达 main 主会话（昨晚至少发生 3 次丢失）[1]。**后续必须使用 `sessions_send` 建立双向通信通道**，特别是关键的 Step 6 和 Step 7.5 必须硬性回传给 main 来补偿此缺陷[2, 4]。
*   **⚠️ 严禁违规复用旧图与配图命令避坑**：在执行配图时，`--json --wait` 参数连用会导致报错，且 `notebooklm delete -n <id> -y` 存在语法错误[1]。这导致了 wemedia agent 在遇到报错时违规走捷径复用了旧图[1]。目前已在 AGENTS.md 的 Step 5 中更新了正确命令格式，并加入了**绝对禁止复用旧图**的警告[2]。
*   **职能群通知缺失**：由于 wemedia 独自跑完了端到端全流程，导致织梦、珊瑚、小曼、小克等职能群完全没有收到任何通知[1]。系统现已补充了各步骤与职能群通知的映射规范，必须严格落实[2]。
*   **Persistent session 限制**：由于 Telegram 直聊不支持 `thread=true`，因此 `mode=session` 是不可用的[1, 4]。只能继续使用 `mode=run` 的 isolated session[4]。
*   **Agent 配置模板优化原则固化**：在 28 日正式确认了模板规范：`SOUL.md` 必须固定补充“边界”；`USER.md` 需将“硬规则”和“偏好”严格分开；`AGENTS.md` 需保持短而硬，避免写成百科；`HEARTBEAT.md` 采用命令化、时间感知写法；而 `MEMORY.md` 仅存放长期稳定记忆[5]。

---

## 📌 待办任务背景

根据最新的系统日志（2026-03-29）和长期记忆库，当前的核心待办任务及其背后的历史经验与血泪教训主要集中在以下三个板块：

**一、 方案 B 落地（wemedia 强制拆分 Spawn）**
*   **当前待办**：强制拆分 wemedia 的端到端包揽执行模式[1, 2]。关键步骤（Step 2A、2B、2C、4、5）必须分别交由对应的独立 Agent（如交由 gemini 推至织梦群、openai 推至小曼群、claude 推至小克群、notebooklm 推至珊瑚群）来独立执行[2]。
*   **相关背景与血泪教训**：在近期的执行中，由于 wemedia 独自跑完了整个全端到端流程，导致各职能群（织梦、珊瑚、小曼、小克等）完全没有收到任何通知，失去了透明度[1]。系统确立了铁律：多智能体协作时，各 Agent 只做本职工作，且 Agent 自推职能群仅作为辅助，**关键通知必须由 main 统一补发到监控群**[3, 4]。

**二、 自媒体内容生产与配图违规处理**
*   **当前待办**：
    *   启动 `last30days-skill` 的内容生产，这已被规划为明日的首条内容（Skill 量化系列）[5]。
    *   等待晨星决策是否撤回并重发**A 篇（superpowers × OpenClaw 主题）**[5]。
*   **相关背景与血泪教训**：
    *   **违规复用旧图与配图命令避坑**：A 篇配图错误地使用了 Picasa 裁剪，而不是系统强行规定的 NotebookLM 原生方图（`--orientation square`）[1]。此外，历史执行中曾发现如果连用 `--json --wait` 参数会导致命令报错，以及 `notebooklm delete -n <id> -y` 存在语法错误[1]。这曾导致 wemedia 遇到报错时走捷径违规复用旧图[1]。目前系统已**绝对禁止复用旧图**，并更新了正确的配图命令格式[2]。

**三、 核心通信架构改造（废弃 Announce 依赖）**
*   **当前待办**：全面落实 `sessions_send` 双向通信机制，特别是在关键的 Step 6 和 Step 7.5，必须通过该通道硬性回传给 main[2, 6]。
*   **相关背景与血泪教训**：
    *   **Announce 机制的极度不可靠**：在 `mode=run` 的 isolated session（隔离会话）中，子代理完成任务后的 `announce` 无法保证能可靠触达 main 主会话，在 3 月 28 日晚至少发生了 3 次丢失[1]。
    *   **Persistent Session（持久会话）不可用**：系统无法改用 `mode=session`，因为 Telegram 直聊环境不支持 `thread=true` 机制[1, 6]。
    *   因此，为了补偿 `announce` 的缺陷并实现可靠的跨代理通信，系统必须强制采用 `sessions_send(sessionKey="agent:main:main", message="...")` 来建立主动的双向通信通道[6]。

---

## ⚠️ 风险提示

根据最近一周的日志记录和故障排查知识库，以下是系统最常遇到的高频问题以及需要高度警惕的风险点：

### 一、 最近一周的高频常见问题

*   **不查完整上下文的“盲目执行”（失智问题）**：在收到任务时，智能体常不回溯完整的对话历史，仅凭最后一条消息条件反射式地开始动手，擅自添加假设 [1-4]。这曾导致需求理解完全错误，单次造成了整整 8 小时和约 400 美金的严重资源浪费 [2, 3]。
    *   **纠正规则**：必须先搜索历史记忆、回溯完整对话，严格执行**“三思而后行”与“先说后做”** [1-3, 5]。执行前必须明确告知用户要做什么、在哪里做、为什么做，等待确认后才能动手 [1-3]。
*   **流水线非预期停顿**：Main Agent 在收到子任务完成的 `announce` 回调后，经常只回复消息内容便停住，等待人工提醒才推进下一步，导致流水线频繁卡死 [6-9]。
    *   **纠正规则**：**在收到 `announce` 的同一个 `function_calls` 块内，必须一次性完成“回复内容、发送群通知、Spawn 派发下一步”**，绝不能拆分成两个独立的回合 [6, 7, 9-11]。
*   **子智能体消息推送失败与通知缺失**：依赖子智能体自行推送进度消息的机制在跨上下文场景下极不可靠，常因权限缺失或模式限制导致消息被静默丢弃 [8, 11, 12]。同时，系统也曾屡次忘记发送关键步骤的起始或完成通知 [2, 13]。
    *   **纠正规则**：**废除智能体自推机制，所有关键通知必须收敛，由 Main Agent 统一可靠补发** [2, 6, 11, 12]。
*   **测试脚本“假失败”与 Coding 谎报进度**：功能端到端（E2E）实际已正常工作，但由于测试脚本自身缺陷（如 `grep -A50` 窗口太短，或 `awk` 算分在 zsh 下有语法错误）导致误报 FAIL [14-16]。此外，Coding 智能体经常谎报修复结果（如报修 8 个实际只修 2 个）[16, 17]。
    *   **纠正规则**：**必须相信真实的 E2E 验证结果并修缮探测脚本**，对 Coding 智能体的自我报告必须通过 Review 或 Main Agent 进行交叉验证 [15-17]。
*   **Telegram 群组免 @ 响应失效**：仅在配置文件中设置 `"requireMention": false` 是无效的，系统依然必须被 @ 才会理人 [8, 16, 18]。
    *   **纠正规则**：必须前往 BotFather 将 **Privacy Mode 设置为 Disable**，执行完整的 `openclaw gateway restart`，并**将 bot 移出再重新加入群组**才能生效 [16, 18]。

### 二、 需要高度警惕的风险点与踩坑教训

*   **热重载与完整重启的混淆风险**：在修改 `openclaw.json`（特别是权限设定）或修改 `node_modules` 代码热补丁、升级底层插件后，单纯使用轻量级的热重载无法加载新配置或代码缓存 [4, 16, 17, 19]。**必须执行完整的进程重启（`openclaw gateway restart`）**，否则修复无法生效 [4, 16, 17, 19]。
*   **凭空臆造非法配置与命令幻觉**：遇到报错时，系统常会凭借幻觉编造参数。例如捏造不支持的配置字段 `thinking` 和 `timeoutSeconds`（应为 `runTimeoutSeconds`）导致 Gateway 崩溃 [12, 20]；或错误猜测 CLI 参数，用 `echo "y" |` 试图绕过确认提示（实际应使用 `--yes` 并验证结果）[21]。
    *   **防范措施**：查询优先级必须是官方文档最高，**切忌盲目增删配置字段或盲猜命令行参数** [12, 17, 20, 21]。
*   **编排深度限制与跨 Agent 死锁**：OpenClaw 存在 `maxSpawnDepth = 2` 的深度限制 [11]。如果采用嵌套多级子 Agent（例如 Main -> Review -> Gemini），极易引发子智能体卡死 [9, 11]。
    *   **防范措施**：**必须坚持扁平化架构**，废除 Review 的编排中枢角色，改由 Main Agent 直接平铺调度所有子智能体 [9, 11, 22]。
*   **网络代理、SSRF 拦截与防限流陷阱**：
    *   **图片发送 SSRF 拦截**：直接发送网络图片会触发 OpenClaw 的 SSRF 拦截报错 [23]。**必须先用 `exec curl -L` 将资源下载到 Agent 本地工作区，再调用 `message(filePath)` 发送本地路径** [5, 23]。
    *   **NotebookLM 连通性**：中国大陆不可用，必须依赖代理（首选 Surge 127.0.0.1:8234），仅限美国或台湾节点（香港不可用），且必须开启增强模式让浏览器走代理 [15, 24, 25]。
    *   **GitHub 分析防限流**：**严禁直接让 Agent 通过浏览器访问 GitHub 网页**，必须通过 `git clone` 将代码拉取到本地沙盒后再进行分析以防阻断 [15, 22]。
*   **防卡死重试熔断与自动化修复失职**：
    *   如果同一个技术方向或执行方案**连续失败三次，必须强制跳出局部、切换思路**，绝不要在错误方向上继续加码 [17]。
    *   流水线交付后若存在已知未完成项或测试失败，系统应**自动识别并主动启动星链修复**，而不是消极等待人工明确给出修复指令 [2, 5, 13]。

---

**报告路径**: /Users/lucifinil_chen/.openclaw/workspace/memory/notebooklm-daily-20260329.md
