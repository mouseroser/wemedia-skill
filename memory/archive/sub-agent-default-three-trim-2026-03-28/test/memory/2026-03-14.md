- 2026-03-14T14:38:15.657Z [other:agent:test] agent=test source=auto-capture 发现一个关键口径风险：`parsePluginConfig` 接收的是 **runtime plugin config**，不是整个 `openclaw.plugin.json` manifest。我要直接核对 `~/.openclaw/openclaw.json` 里的 `plugins.entries["memory-lancedb-pro"].config`，看 `layer3Fallback` 到底有没有进真实运行态。
