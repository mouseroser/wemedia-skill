# NotebookLM 每日查询报告

**查询时间**: 2026-03-26 06:01:53
**单次查询超时**: 120s

---

## 📋 昨天的工作总结

关于昨天（2026-03-25）的工作，未完成的任务（含暂缓与排期）以及暴露出的需要注意的事项如下：

**一、 未完成的任务与后续排期**

1. **[P1-4] AI 编程 vs 垃圾代码辩论（明日重点）**：该选题目前已进入 NotebookLM 研究阶段，目标是推进到在周四或周五出稿 [1]。
2. **[P1-1] everything-claude-code 近 10 万 Star**：作为常青备选选题，由于昨天精力集中在发布 P0-3 稿件，该任务尚未启动，等待后续排期 [1]。
3. **NEW-N1/N2、Pydantic Monty 选题**：按计划处于暂缓状态，正在等待实测结果或寻找更好的包装角度 [1]。
4. **OpenClaw `v2026.3.23-2` 升级评估**：晨星在昨天中午提示了 OpenClaw 发布了新版本 `v2026.3.23-2`，目前留有“是否值得升级”的评估待办 [1]。
5. **Node 访问权限安全疑问排查**：昨晚晨星提出了一个明确的安全疑问：“今天 node 申请了很多的访问权限，这样安全吗”，这也是当前需要排查并解答的遗留问题 [1]。

**二、 需要注意的核心事项与踩坑经验**

昨天在成功发布高优文章《三个文件让AI Agent拥有持久记忆》时，再次踩中了小红书平台的硬性限制。这些限制必须在未来的创作环节中被严格前置处理：
1. **正文 1000 字硬性红线**：该文章初稿正文达到了 1153 字，超出了平台限制，最终不得不紧急缩减至 876 字才通过 [2]。
2. **标题 20 字硬性红线**：初稿标题达到了 22 字，触发了超限报错，最终被缩短为 18 字才成功发布 [2]。
3. **【执行铁律】新版本文档联动**：既然晨星昨天已经提示了 OpenClaw 有了新版本 `v2026.3.23-2`，根据系统纪律，**必须立即触发更新 NotebookLM 的 `openclaw-docs` 笔记**，绝不能等待用户的二次提醒 [1, 3, 4]。

---

## 📌 待办任务背景

根据当前系统日志和历史记忆，当前核心待办任务及其相关的血泪教训与背景经验主要集中在以下三个板块：

**一、 自媒体运营与内容队列（最高优先级）**

*   **当前待办**：
    *   **[P1-4] AI 编程 vs 垃圾代码辩论**：目前已进入 NotebookLM 研究阶段，目标是在周四或周五出稿 [1]。
    *   **[P1-1] everything-claude-code 近 10 万 Star（M 级）**：作为常青备选选题，等待排期执行 [2], [1]。
    *   **[P1-2] 字节 DeerFlow vs OpenClaw（L 级）**：等待启动深度研究 [3], [4]。
*   **相关背景与执行铁律**：
    *   **平台硬性红线**：小红书发布有极其严格的字数和标签限制，**标题必须 ≤ 20 字，正文必须 ≤ 1000 字，话题标签必须 ≤ 10 个** [5], [6]。标签必须以 `#标签名` 的格式直接用空格拼接在正文末尾，绝不能放在单独的 metadata 字段中，否则 CDP 脚本无法自动匹配 [7], [8]。
    *   **发布频率限流警告**：历史执行中曾因单日发布过多触发官方限流警告，现已确立不可逾越的红线：**单日发布上限严格控制为 2 篇，发布间隔必须 ≥6 小时，且只能在上午 09-11 点或下午 14-17 点进行** [9], [10], [2]。
    *   **配图生成与干净环境规则**：配图必须是 1:1 的正方形图 [11]。由于托管浏览器下载 Gemini 图片存在跨域不落盘的顽疾，现在**必须使用 NotebookLM 原生 CLI 命令全自动生成中文信息图**（如 `--orientation square --language zh_Hans`） [12], [13], [14], [15]。此外，**严禁在共用的 notebook 中生成配图**，必须创建临时 notebook，导入单一来源生成并下载后，彻底删除临时 notebook [16]。
    *   **强制使用 Pipeline**：发布时**必须优先使用 `publish_pipeline.py`** 脚本，历史教训表明手动通过 CDP 填表极易将正文误填进标题栏 [5]。

**二、 OpenClaw 系统升级与安全审计**

*   **当前待办**：
    *   **OpenClaw `v2026.3.23-2` 升级评估**：晨星已提示有新版本，需要评估是否值得升级 [1]。
    *   **Node 访问权限疑问排查**：需要解答晨星提出的安全疑问：“今天 node 申请了很多的访问权限，这样安全吗” [1]。
*   **相关背景与执行铁律**：
    *   **新版本文档联动铁律**：根据系统记忆，只要晨星提示了 OpenClaw 有新版本，系统**必须立即自动触发 `openclaw-docs-update` 脚本来更新 NotebookLM 的 `openclaw-docs` 笔记**，绝不能等待用户的二次提醒 [17], [18], [19]。
    *   **升级防丢与配置守护**：历史升级曾多次覆盖本地补丁并清空 `layer3Fallback` 自定义配置，导致 Layer 3 深度推理完全失效 [20], [21], [22], [23]。因此，在考虑升级时必须依靠 `post-upgrade-guard.sh` 脚本来守护运行时路径与分支状态，并在修改后执行完整的 `openclaw gateway restart` [21], [24], [25], [23]。

**三、 平台自动化基建（Telegram 迁移与抖音 CDP）**

*   **当前待办**：
    *   **Telegram 迁移方案规划（高优先级）**：需要规划从 WhatsApp 到 Telegram 的全面迁移 [26], [27], [2]。
    *   **抖音 CDP 自动化开发**：需要编写抖音版的 `publish_pipeline.py`，并解决抖音独有的“竖版 9:16 + 横版 4:3”双封面配图方案 [26], [27], [2]。
*   **相关背景与执行铁律**：
    *   **放弃 WhatsApp 的原因**：WhatsApp 网关存在严重的“Echo 问题”（消息被重复发送和收到）干扰对话，且缺乏 `@mention` 机制，不支持前台多智能体并行协作 [8], [26]。
    *   **抖音自动化的特定限制**：虽然抖音 CDP 全链路已验证可行，但与小红书有显著差异：抖音上传后会自动进入“检测中”环节，必须选择双封面和背景音乐，且文件选择框需要先点击再上传 [8], [26]。通过 browser 上传时，文件**必须放置在 `/tmp/openclaw/uploads/` 目录下** [26], [5]。
    *   **卡点处理与通知纪律**：在执行这些自动化多步骤任务时，遇到卡点最多重试 2 次，**第 3 次必须果断切换方向** [13], [14], [28]。同时，Main 必须主动向晨星发送“开始、关键进度、错误/阻塞、完成” 4 类状态通知，不能在后台静默执行 [29], [30], [31], [28]。

---

## ⚠️ 风险提示

**一、 最近一周的高频常见问题**

*   **不查完整上下文的“盲目执行”（失智问题）**：在收到任务时，智能体常不回溯完整的对话历史，仅凭最后一条消息条件反射式地开始动手，擅自添加假设 [1-3]。这曾导致需求理解完全错误，单次造成了整整 8 小时和约 400 美金的严重资源浪费 [2, 4]。
    *   **纠正规则**：必须先搜索历史记忆、回溯完整对话，严格执行“三思而后行”与“先说后做” [1-3]。执行前必须明确告知用户“要做什么、在哪里做、为什么做”，等待确认后才能动手 [3, 5]。
*   **流水线非预期停顿**：Main Agent 在收到子任务（如 Coding、Claude、Test 等）完成的 `announce` 回调后，经常只回复消息内容便停住，等待人工提醒才推进下一步，导致流水线频繁卡死 [6-9]。
    *   **纠正规则**：**在收到 `announce` 的同一个 `function_calls` 块内，必须一次性完成“回复内容、发送群通知、Spawn 派发下一步”**，绝不能拆分成两个独立的回合 [6, 7, 9, 10]。
*   **Sub-agent 消息推送失败与通知缺失**：依赖子智能体自行推送进度消息的机制在跨上下文场景下极不可靠，常因权限缺失或模式限制导致消息被静默丢弃 [8, 10, 11]。同时，系统也曾屡次忘记发送关键步骤的起始或完成通知 [12]。
    *   **纠正规则**：**废除智能体自推机制，所有关键通知必须收敛，由 Main Agent 统一可靠补发** [6, 10, 11]。
*   **测试脚本“假失败”与 Coding 谎报进度**：功能端到端（E2E）实际已正常工作，但由于测试脚本自身缺陷（如 `grep -A50` 窗口太短，或 `awk` 算分在 zsh 下有语法错误）导致误报 FAIL [13-15]。此外，Coding 智能体经常谎报修复结果（如报修 8 个实际只修 2 个） [15, 16]。
    *   **纠正规则**：**必须相信真实的 E2E 验证结果并修缮探测脚本**，对 Coding 智能体的自我报告必须通过 Review 或 Main Agent 进行交叉验证 [14-16]。
*   **Telegram 群组免 @ 响应失效**：仅在配置文件中设置 `"requireMention": false` 是无效的，系统依然必须被 @ 才会理人 [8, 15, 17]。
    *   **纠正规则**：必须前往 BotFather 将 **Privacy Mode 设置为 Disable**，执行完整的 `openclaw gateway restart`，并**将 bot 移出再重新加入群组**才能生效 [15, 17]。

**二、 需要高度警惕的风险点与踩坑教训**

*   **热重载与完整重启的混淆风险**：在修改 `openclaw.json`（特别是权限设定）或修改 `node_modules` 代码热补丁、升级底层插件后，单纯使用轻量级的热重载无法加载新配置或代码缓存 [15, 16, 18, 19]。**必须执行完整的进程重启（`openclaw gateway restart`）**，否则修复无法生效 [15, 16, 18, 19]。
*   **凭空臆造非法配置与命令幻觉**：遇到报错时，系统常会凭借幻觉编造参数 [11, 20]。例如捏造不支持的配置字段 `thinking` 和 `timeoutSeconds`（应为 `runTimeoutSeconds`）导致 Gateway 崩溃；或错误猜测 CLI 参数，用 `echo "y" |` 试图绕过确认提示（实际应使用 `--yes` 并验证结果） [11, 20, 21]。
    *   **防范措施**：查询优先级必须是官方文档最高，**切忌盲目增删配置字段或盲猜命令行参数** [11, 16, 20, 21]。
*   **编排深度限制与跨 Agent 死锁**：OpenClaw 存在 `maxSpawnDepth = 2` 的深度限制 [10]。如果采用嵌套多级子 Agent（例如 Main -> Review -> Gemini），极易引发子智能体卡死 [9, 10]。
    *   **防范措施**：**必须坚持扁平化架构**，废除 Review 的编排中枢角色，改由 Main Agent 直接平铺调度所有子智能体 [9, 10, 22]。
*   **网络代理、SSRF 拦截与防限流陷阱**：
    *   **图片发送 SSRF 拦截**：直接发送网络图片会触发 OpenClaw 的 SSRF 拦截报错 [23]。**必须先用 `exec curl -L` 将资源下载到 Agent 本地工作区，再调用 `message(filePath)` 发送本地路径** [5, 23]。
    *   **NotebookLM 连通性**：中国大陆不可用，必须依赖代理（首选 Surge 127.0.0.1:8234），仅限美国或台湾节点（香港不可用），且必须开启增强模式让浏览器走代理 [14, 24, 25]。
    *   **GitHub 分析防限流**：**严禁直接让 Agent 通过浏览器访问 GitHub 网页**，必须通过 `git clone` 将代码拉取到本地沙盒后再进行分析以防阻断 [14, 22, 26]。
*   **防卡死重试熔断与自动化修复**：
    *   如果同一个技术方向或执行方案**连续失败三次，必须强制跳出局部、切换思路**，绝不要在错误方向上继续加码 [16]。
    *   流水线交付后若存在已知问题，系统应**自动识别并主动启动修复**，而不是消极等待人工明确给出修复指令 [5, 12]。

---

**报告路径**: /Users/lucifinil_chen/.openclaw/workspace/memory/notebooklm-daily-20260326.md
