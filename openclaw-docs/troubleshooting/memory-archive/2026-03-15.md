# 2026-03-15

- 【memory-lancedb-pro 上游跟进】PR #206（`feat/layer3-notebooklm-fallback`）已无冲突且 GitHub 显示 `MERGEABLE`；当前剩余卡点不是代码回归，而是 fork PR 的 GitHub Actions 审批门槛（workflow 状态为 `action_required`）。
- 【Claude Code Review 假失败根因】`Claude Code Review` 在 fork PR 的 `pull_request` 触发下拿不到 OIDC / auth 上下文，会在真正审查前直接失败；关键报错为：`Unable to get ACTIONS_ID_TOKEN_REQUEST_URL env variable` / `Could not fetch an OIDC token`。
- 【已做修复】为避免 fork PR 上的假红灯，已在分支上提交修复 commit `360483e`：对 fork PR 跳过 `claude-review` job；并将该修复拆成独立上游 PR #210：`ci: skip Claude Code Review on fork PRs`（分支 `fix/skip-claude-review-on-fork-prs`，commit `76505c6`）。
- 【PR 说明已补充】已在 PR #206 留 comment，明确说明：此前 claude-review 红灯不是代码问题，而是 fork PR 权限/OIDC模型问题；本次 PR 的 build / fallback test / manifest regression / 本地 runtime 验证已完成。
- 【后续跟进机制】已新增 cron `check-memory-lancedb-pr-status`，每天 09:00（Asia/Shanghai）检查 PR #206 与 PR #210；仅在出现有意义状态变化（合并、解锁 checks、需要 maintainer 动作等）时再通知晨星，否则静默。
- 【Layer 3 恢复】2026-03-15 凌晨：OpenClaw 升级到 `v2026.3.13` 后，活动 runtime 配置里的 `plugins.entries["memory-lancedb-pro"].config.layer3Fallback` 被清掉；同时 workspace 插件工作树停留在 `fix/skip-claude-review-on-fork-prs`，导致 `layer3Fallback` 相关代码/manifest 也不可见。已切回 `feat/layer3-notebooklm-fallback` 分支，恢复 runtime 配置块（`enabled:true`, `agent:notebooklm`, `notebook:memory-archive`, `timeout:45` + triggers），并通过 gateway `config.patch` 重启加载。随后本地验证通过：`npm run build` ✅、`node --test test/layer3-fallback.test.mjs` ✅、`node test/plugin-manifest-regression.mjs` ✅。
- 【稳态防护已落地】为避免再次出现"切 PR 分支影响 live runtime"与"升级后 Layer 3 配置被裁掉"的问题，已新增专用 runtime worktree：`~/.openclaw/runtime-plugins/memory-lancedb-pro`（分支 `runtime/layer3-fallback-active`），并把 OpenClaw `plugins.load.paths` 切到该路径；PR #210 使用独立 worktree：`~/.openclaw/worktrees/memory-lancedb-pro/pr-210`。同时在 `HEARTBEAT.md` 增加升级 / Layer 3 守护规则，并初始化状态文件 `~/.openclaw/state/layer3-fallback-guard.json`，要求每次心跳检查版本变化、runtime load path 与 `layer3Fallback` 配置，异常时立即自愈并重启 gateway。
- 【主执行计划同步更新】01:00 将今晚所有工作纳入主执行计划（`~/.openclaw/todo/master-execution-plan.md`）：新增 4C（Layer 3 + PR 管理，8 项已完成 6）、4D（升级修复，4 项全完成）、4E（L2+L3 联动 7 天观察期，7 项）、4F（星链 Spec-Kit 实施，6 项）。总任务 42→61，已完成 21→28（46%）。
- 【P2.3 正式关闭】决策：放弃 builtin memorySearch 双轨路线。当前最终架构为 L2 快速召回（memory-lancedb-pro hybrid/MMR/rerank）→ L3 深度推理（NotebookLM 自动降级）。builtin memorySearch 不再作为第二召回层。
- 【星链方案 C 待纳入】晨星指出"方案 C"（异步投递模式）需要加入 TODO 计划。方案 C 来自 NotebookLM 深度研究报告（`agents/notebooklm/reports/starchain-memory-fallback-research.md` §2.3）：Layer 2 即时返回 + Layer 3 异步派发 + 结果通过 Telegram DM 推送。目前尚未正式纳入主执行计划的 4F（Spec-Kit 实施），需要在下次会话中确认晨星要求的具体内容后补入。
- 【晨星反馈待跟进】晨星说"2不对"，经上下文分析应是指 4F 中关于方案 C（异步投递）的任务描述不准确或遗漏，而非 Spec-Kit Phase 1 的四项裁决。需要晨星确认后修正。
