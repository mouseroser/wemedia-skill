# 2026-02-26 工作日志

## agent-teams-openclaw skill 完整搭建

### 完成的工作
1. 阅读 GitHub spec-kit (github.com/github/spec-kit) 完整文档，理解 SDD 方法论
2. 为 6 个 agent 创建 AGENTS.md + SOUL.md（review/coding/test/docs/monitor-bot/brainstorming）
3. 更新 skill 的 SKILL.md、pipeline-v1-8-contract.md、PIPELINE_FLOWCHART_V1_8_EMOJI.md
4. 创建 pipeline-v1.8.md（v1.5 样式的完整流程文档）
5. 集成 spec-kit 到 Step 1.5（specify → plan → tasks → analyze）
6. 配置 openclaw.json：
   - agents.list[main].subagents.allowAgents = [review, coding, test, docs, monitor-bot, brainstorming]
   - agents.list[review].subagents.allowAgents = [coding, test, docs, monitor-bot, brainstorming]
   - agents.defaults.subagents.maxSpawnDepth = 2
   - agents.defaults.subagents.maxChildrenPerAgent = 8

### 模型策略（晨星确认）
- 日常编码/测试：sonnet/medium + codex/medium
- 交叉审查首轮：sonnet/medium 审 codex 代码 | codex/high 审 sonnet 代码
- 分歧仲裁：opus/medium + codex/xhigh
- 复杂架构/算法设计：opus/high + codex/xhigh
- 修复递进：sonnet/medium → opus/medium → opus/high
- 核心原则：默认低配跑，按需升级

### 踩坑与解决
1. agents_list 只返回 main → 需要配置 subagents.allowAgents
2. review 自己改文件违反流程 → AGENTS.md 加 ⛔ 硬性约束（绝不自己 coding/test）
3. depth-2 sub-agent 不注入 AGENTS.md → review spawn 时在 task 中附带推送指令（chat ID + message 用法）
4. 各 agent 没推送消息 → review 的 spawn 推送指令模板解决
5. review announce 中英文混用 → SOUL.md 加语言规则

### 端到端测试结果（L1 快速通道）
- ✅ review Step 1 分级 → 推送监控群+审核群
- ✅ coding agent 修复文件 → 推送编程群+监控群
- ✅ test agent 验证 → 推送测试群+监控群
- ✅ review Step 3 审查 JSON verdict → 推送审核群
- ✅ review Step 7 交付 → 推送监控群
- 每个 agent 独立推送到自己的群，符合原始流程设计

### 语言规则（晨星确认）
- 晨星 ↔ 小光：中文
- 小光与 claude（opus/sonnet）和 codex 模型交流：英文
- 其他场景：中文
- review 推送 Telegram 群：中文
- review announce 回报 main：中文
- review spawn task 给 coding/test：英文

## 模型策略更新（00:40-00:49）

### 晨星提供的完整模型策略表
按 Step 细化模型配置，核心原则：默认低配跑，按需升级。

| Step | 阶段 | Claude | Think | Codex | Think |
|------|------|--------|-------|-------|-------|
| 1 | 分级 | Sonnet | medium | — | — |
| 1.5 | Spec-Kit | Sonnet | medium | Codex | medium |
| 3 | 审查首轮 | Sonnet | medium | Codex | high |
| 3 | 分歧仲裁 | Opus | medium | Codex | xhigh |
| 4 R1-R2 | 修复循环 | Sonnet | medium | Codex | medium |
| 4 R3 | 兜底修复 | Opus | high | Codex | xhigh |
| 7 | 汇总 | Sonnet | low | — | — |

### 关键变化
- R2 从 opus/medium 降为 sonnet/medium（前两轮保持低配快速迭代）
- TF-2 从 opus/medium 降为 sonnet/medium
- Epoch R2 同步降为 sonnet/medium
- opus 只在 R3 兜底、分歧仲裁、复杂架构时使用

### 更新范围
- review SOUL.md + AGENTS.md
- brainstorming SOUL.md + AGENTS.md
- coding/test SOUL.md
- pipeline-v1.8.md
- pipeline-v1-8-contract.md
- PIPELINE_FLOWCHART_V1_8_EMOJI.md
- 全部验证无残留旧配置

## 踩坑：review 跳过 Step 2.5 冒烟测试（01:00）

### 问题
L2 测试中 review 从 Step 2 coding 完成后直接跳到 Step 3 审查，跳过了 Step 2.5 冒烟测试。

### 原因
review AGENTS.md 里没有 Step 2 和 Step 2.5 的独立章节，只在推送列表和协作对象表里提到了。

### 解决
在 review AGENTS.md 中 Step 1.5 和 Step 3 之间补充了 Step 2（开发执行）和 Step 2.5（冒烟测试）的完整指令，包括：
- 冒烟 PASS → Step 3
- 冒烟 FAIL → coding 自修复（最多 2 次）
- 2 次仍 FAIL → 通知监控群
- ⚠️ 冒烟测试不可跳过

## 流水线测试结果汇总

- ✅ L1 快速通道：端到端通过
- ✅ L2 标准通道：端到端通过（修复 Step 2.5 跳过问题后验证通过，监控告警群消息确认）
- ⏳ L3 复杂通道：待测试

## 异模型交叉修正（晨星确认）

### 问题
原配置中 Review A 和 coding 使用同一模型家族，不是真正的交叉审查。测试也与 coding 同模型。

### 修正原则
Claude 开发 → GPT 审 + GPT 测，GPT 开发 → Claude 审 + Claude 测。

### 新配置
- Type A：coding(sonnet/Claude) → review(codex/high) + brainstorming(codex/medium) + test(codex/medium)
- Type B：coding(codex/GPT) → review(sonnet/medium) + brainstorming(sonnet/medium) + test(sonnet/medium)

### 更新文件（6 个）
1. review AGENTS.md — Step 1 分型 + Step 3 审查模型
2. review SOUL.md — 模型配置 + 交叉审查描述
3. test SOUL.md — 改为异模型
4. test AGENTS.md — 添加异模型原则
5. brainstorming AGENTS.md — Step 3 Review B 模型说明
6. pipeline-v1-8-contract.md — Section 2 + Section 12
7. PIPELINE_FLOWCHART_V1_8_EMOJI.md — Step 3 + Step 5

## Step 2.5 冒烟测试流程统一（06:45）

### 发现的矛盾
1. 谁执行冒烟测试：流程图+合约说 coding 自执行，review AGENTS.md 说 spawn test agent
2. 2次FAIL后去哪：流程图+合约说进 Step 4，review AGENTS.md 说进 Step 3

### 统一结果
- 冒烟测试由 coding agent 自执行（不 spawn test agent）
- 2次FAIL后进 Step 4 修复循环（携带失败日志）
- test agent 从 Step 5 正式测试开始

### 更新文件
1. review AGENTS.md — Step 2.5 改为 coding 自执行 + FAIL→Step 4；协作对象表 test 去掉 Step 2.5
2. coding AGENTS.md — Step 2.5 改为自执行冒烟 + 补推送指令 + FAIL→Step 4
3. test AGENTS.md — Step 2.5 标注冒烟由 coding 负责

## review 语言硬性约束加强（06:45）

### 问题
review agent announce 回报 main 时输出英文（"Waiting for the coding agent to complete..."）

### 解决
- review AGENTS.md — 硬性约束区新增「语言硬性约束」，跟"不能自己写代码"同级
- review SOUL.md — 语言规则从 ⚠️ 升级为「最高优先级」，违反等同于流程错误

## Step 3 从双重审查改为交叉审查（07:00）

### 晨星指示
- 不再有 Review A + Review B 两轮审查
- 单轮异模型交叉审查：Claude 写的 → Codex 审，Codex 写的 → Claude 审
- 分歧仲裁触发条件：审查方提出问题 + 编写方反驳（不是两个审查者之间的分歧）
- brainstorming 不再参与 Step 3

### 更新文件
1. review AGENTS.md — Step 3 重写为单轮交叉审查 + 新仲裁逻辑；协作对象表 brainstorming 去掉 Step 3
2. review SOUL.md — 交叉审查原则更新
3. brainstorming AGENTS.md — 删除 Step 3 Review B 章节 + 协作对象表更新
4. brainstorming SOUL.md — 核心职责去掉 Step 3
5. pipeline-v1-8-contract.md — Section 4 Step 3 + Section 12 模型交叉原则
6. PIPELINE_FLOWCHART_V1_8_EMOJI.md — Step 3 部分
7. workspace 副本 pipeline-v1-8-contract.md 同步

## PASS_WITH_NOTES 降级测试（21:47）

晨星选择测试第3项：PASS_WITH_NOTES 降级流程。

### 背景回顾
- L1 + L2 已全部验证通过
- 之前已测试 TF（测试失败）回审流程并通过
- 本次测试 PASS_WITH_NOTES 降级

### 待测试
- Step 3 审查给出 PASS_WITH_NOTES 时，代码如何降级处理
- 需要构造一个有 notes 但非阻塞问题的代码场景

## Epoch 回滚测试排查与改进（22:00）

### 排查结果
- 第一次 Epoch 测试（/tmp/epoch-test/）实际上完整走通了 R1→R2→R3→Epoch→重新开发→交付
- OpenClaw 的 announce 机制会在子 agent 完成后重新唤醒 review 继续执行，mode=run 支持多轮串行
- 真正的问题：coding agent API 凭证错误 → review 违反硬性约束自己改了代码
- 第二次测试（v2）review 走到 R3 spawn coding 后结束，可能是 session 状态冲突

### 执行的改进
1. 全局 model fallback 已配置（opus → minimax → codex → gpt → gemini）
2. review AGENTS.md 新增硬性约束：子 agent 失败时必须重试 spawn（最多3次），绝不自己代替执行；3次仍失败 → 推送监控群 + HALT
3. coding agent 不单独设 primary model，继承全局默认 + fallback

### 踩坑
- sessions_spawn 是非阻塞的，立即返回
- mode=run 下 announce 机制会重新唤醒 parent session 继续处理
- 短时间内多次 spawn 同一 agent 可能导致 session 状态冲突

## wemedia agent 自媒体管家搭建

### 完成的工作
1. 创建 workspace-wemedia/ 完整目录结构
2. AGENTS.md — 职责定义（内容创作、排期发布、数据监控、多平台分发、评论互动）、工作流闭环、沟通规则、安全红线
3. SOUL.md — 性格设定（专业、有网感、数据敏感、主动但不越界）
4. platforms/ — 小红书、TikTok/抖音、知乎三平台配置（特性、规范、风格、指标）
5. templates/ — 三平台内容模板（小红书种草文、抖音脚本、知乎长文）
6. content-calendar/2026-02.md — 月度排期表
7. analytics/weekly-template.md — 数据周报模板
8. openclaw.json — main 和 review 的 allowAgents 加入 wemedia
9. 各平台账号信息标记 [待填写]，等晨星提供

### 配置确认
- 模型：minimax/MiniMax-M2.5，thinking: high
- Telegram 群：-5146160953
- binding 已配置，requireMention: false

## review Step 7 通知晨星缺失修复

### 问题
pipeline-v1.8.md 流程图画了 review → main → 晨星 的通知链路，但 review AGENTS.md 的 Step 7 只写了"输出交付摘要"，没有明确的通知晨星指令。

### 修复
1. Step 7 标题改为「交付汇总 + 通知晨星」，加入推送监控群 + 通知晨星（target: 1099011886）的 message 指令
2. HALT 处理加入通知晨星 + degraded 交付必须走完整 Step 7 通知流程
3. 推送规范列表加入「Step 7 通知晨星（必须）」

### 教训
- 流程图和执行指令必须一一对应，光画图不写指令等于没有
- ⚠️ 晨星要求：以后遇到问题和解决方案要主动记录到 memory，不用问

## 新增 nano-banana 和 gemini agent（23:00）

### 配置
- nano-banana：gemini/gemini-3-pro-image-preview，生图群 -5217509070
- gemini：gemini/gemini-3-1-pro-preview，gemini 群 -5264626153
- workspace 已创建，AGENTS.md 已写入

### 踩坑
1. 获取新群 chat ID：polling 模式下 getUpdates 为空，停 gateway 也抓不到。解决：临时把 groupPolicy 改为 open，发消息后从日志 grep 负数 ID
2. groups 配置里不支持 agentId 字段（Unrecognized key），agent 绑定必须用 bindings 数组
3. groups 和 bindings 需要 gateway restart 才生效（不是 dynamic reload）
4. thinking 字段不能直接写在 agents.list[] 里（Unrecognized key），会导致配置加载失败
