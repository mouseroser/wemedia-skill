# Memory Recall Scorecard — 2026-03-26 Round 2

基于 Round 1 补充高价值原子记忆后的复测结果。

## 本轮新增补强

已补入 Layer 2 的高价值原子记忆：
- 托管浏览器偏好
- 统计学表述约束
- Berryxia 外部研究资产定位
- `message(action=send)` 的 `buttons=[]` workaround
- 分层健康检查规则
- 文件工具路径绝对路径规则

---

## Round 2 复测结果

| Query | 预期 | 结果 | 结论 |
|-------|------|------|------|
| 晨星要求默认使用什么浏览器读取 X 网页内容？ | 托管浏览器优先 | 新原子记忆已进入 Top3，且语义准确 | 明显改善 |
| StarChain v2.9 plugin suite 当前能否说具有统计学显著性？ | 不能，只能说工程有效 | 新原子记忆 Top1 直接命中 | 已修正成功 |
| Berryxia 那篇 X 长文在系统里应被当成什么？ | 外部研究资产，不是 canonical | 新原子记忆 Top1 直接命中 | 已修正成功 |
| `message(action=send)` 最近确认过的工具层坑是什么？ | `buttons=[]` workaround | 新原子记忆已成功召回 | 已进入可用态 |
| `read` / `write` / `edit` 工具使用路径时有什么已知坑？ | 不依赖 `~`，优先绝对路径 | 新原子记忆 Top1 直接命中 | 已修正成功 |
| Heartbeat 发现 cron 错误时应该如何判断 memory 是否真的坏了？ | 先做分层判断 | 仍未命中新分层规则，旧 cron / 守护记忆竞争较强 | 仍需继续提纯 |

---

## 对比 Round 1

### 已明显提升
- 新近用户偏好
- 新近决策
- 外部研究资产边界
- 新增工具级 workaround
- 文件工具路径坑

### 仍偏弱
- 分层健康检查规则

### 原因判断
分层健康检查规则仍未顶到前面，说明：
1. 旧的 cron / 升级守护 / memory 故障记忆较多
2. 新规则虽已写入，但“查询词 → 规则表述”之间仍有词面差异
3. 需要后续再补 1-2 条更贴近查询句式的原子记忆，或做旧记忆压噪/提纯

---

## 阶段结论

本轮优化已验证：

1. **补原子记忆 → recall 质量可以快速提升**
2. Layer 2 对“长期稳定记忆”一直较强
3. Layer 2 对“刚形成的新高价值规则”需要主动原子化沉淀
4. 外部研究资产边界、工具坑、最新偏好，都适合用短句型原子记忆增强命中

---

## 下一步建议

### P1
- 继续补“分层健康检查”相关的 1-2 条更贴近查询句式的原子记忆
- 对竞争过强的旧 cron / memory 错误记忆做后续提纯

### P2
- 将 Round 1 / Round 2 的变化收入口头模板，形成“遇新规则先原子化”的固定动作
- 后续定期跑 benchmark，观察新规则进入 Top1 的速度

---

## 一句话结论

Round 2 已经证明这次优化有效：**新近高价值规则经过原子化后，Recall 命中率明显提升；当前主要剩余短板是“分层健康检查规则”的召回排序。**
